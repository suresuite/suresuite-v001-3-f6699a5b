# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env

# TypeScript incremental build artifacts
*.tsbuildinfo

# This project standardizes on npm (package-lock.json). Other lockfiles are
# ignored so a stray `bun install` / `yarn install` can't reintroduce a second
# source of truth — Vercel picks its package manager by which lockfile it finds.
bun.lock
bun.lockb
pnpm-lock.yaml
yarn.lock

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Python
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/

# Python build/test artifacts
*.egg-info/
__pycache__/
*.pyc
.pytest_cache/
build/

# ...except the repo-root build/, where the data contract emits its generated
# artifacts. They are COMMITTED on purpose: `npm run contract:check` compares the
# committed copy against a fresh introspection, so an uncommitted artifact is a
# gate that can never fail (Phase 1 / WP 1.1).
!/build/
/build/*
!/build/schema.introspected.json
!/build/data-contract.generated.json

# Deploy secrets — single source of truth, NEVER commit (see scripts/deploy.env.example)
scripts/deploy.env
deploy.env
