{
  "$doc": "The ratchet `npm run typecheck` enforces. One entry per file that had type errors when the gate was added (Phase 5 / D57). It may SHRINK and may not GROW: a new error in any file, or one more error in a listed file, fails. So does one FEWER, which asks for the number to be lowered — a baseline that records paid debt is a baseline nobody trusts.",
  "$added_by": "Phase 5 / D57 — the gate that did not exist. Sixty-one of the 89 errors here on the day it was written were D57's own, emitted by scripts/data-contract/generate.mjs into a committed artifact since WP 5.2h. Those are fixed; these twenty-eight predate Phase 5 and belong to whoever owns each surface.",
  "files": {
    "src/components/sim/DisruptionScheduleEditor.tsx": {
      "count": 4,
      "why": "`target_type` is a `\"node\" | \"edge\"` union in state and plain `string` where it is parsed. §16 · WP 6.4 records that `_targets` has this discriminator and NO validation behind it, so the type error and the missing CHECK are the same defect seen from two sides. WP 6.4's."
    },
    "src/components/trust/TrustReportPanel.tsx": {
      "count": 6,
      "why": "The generated Supabase client types do not know `ingest_runs`, so `.from('ingest_runs')` resolves to a SelectQueryError union and every field read off it is an error. The client types are regenerated outside this repository; WP 4.4's panel, not Phase 5's."
    },
    "src/components/trust/useProjectFreshness.ts": {
      "count": 1,
      "why": "`.rpc('project_freshness')` — the same stale client types as above, on the RPC side."
    },
    "src/hooks/useErpConnections.tsx": {
      "count": 7,
      "why": "Same cause again, and the largest instance: `project_erp_links` and `ingest_runs` are absent from the generated client types, so two casts are reported as non-overlapping and two more instantiations run away. WP 7.1's connector surface."
    },
    "src/lib/policies/__tests__/dataPlaneAudit.test.ts": {
      "count": 1,
      "why": "`@ts-expect-error` on a JSON import that now resolves. Harmless, and deliberately left: deleting four directives across four suites is a change to four gate files for no behavioural reason, which is not a thing a documentation package should be doing."
    },
    "src/lib/policies/__tests__/ingestSpecParity.test.ts": {
      "count": 1,
      "why": "A literal `string` widened where a `\"duration\" | \"rate\"` union is expected, in a fixture the suite builds for itself."
    },
    "src/lib/policies/__tests__/orgIdentity.test.ts": {
      "count": 1,
      "why": "Unused `@ts-expect-error`, as in dataPlaneAudit.test.ts."
    },
    "src/lib/policies/__tests__/projectMembership.test.ts": {
      "count": 1,
      "why": "Unused `@ts-expect-error`, as in dataPlaneAudit.test.ts."
    },
    "src/lib/policies/__tests__/stalenessOneRule.test.ts": {
      "count": 1,
      "why": "Unused `@ts-expect-error`, as in dataPlaneAudit.test.ts."
    }
  },
  "$paid_down": "Phase 6 / WP 6.3 (A1): five errors across three files, all one surface — the `Provenance` union did not carry `contract`, `NumCell` did not declare the `title` it reads, and the `bulkUpsertOverrides` prop type dropped WP 4.4's `{ seeded }` options argument. 28 -> 23."
}
