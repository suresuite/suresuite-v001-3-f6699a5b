# Touch this file to trigger `.github/workflows/verification-sql.yml`.
# NEVER in the same push as a migration: `supabase-migrations.yml` has no branch
# filter (§4 D31), so a branch push DEPLOYS, both workflows fire on that push
# with no ordering between them, and a §15 run that races a deploy reports a
# database that changed underneath it. FIVE runs have been lost to this across
# three packages. And the trigger has THREE doors — this file, the workflow, and
# `scripts/data-contract/verification-sql.mjs`. A push carrying a migration must
# change NONE of them.
#
# WP 4.1 — BEFORE. Its own push, no migration in it.
# measure: what the `schema_version` bump costs before it is taken —
#      how many live `proposals` carry a v1 `graph_hash` and would be UPDATEd to
#      `expired`/`grounding_drift` by the first read after the deploy (the one
#      reader that writes); how many `model_validations` cards and
#      `project_memory` rows go stale (display-only, reversible); whether every
#      `simulation_runs.dataset_version_id` still resolves; the row counts of
#      the five tier-2 tables v1 does not hash — including §11's settled claim
#      that `hash_network`'s three hold ZERO; and D36's `actor_known: false`
#      rows by table, so the after-run has a before to be a delta of.
WP 4.1 graph_hash blast radius — before
