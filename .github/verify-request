# Touch this file to trigger `.github/workflows/verification-sql.yml`.
# NEVER in the same push as a migration: `supabase-migrations.yml` has no branch
# filter (§4 D31), so a branch push DEPLOYS, both workflows fire on that push
# with no ordering between them, and a §15 run that races a deploy reports a
# database that changed underneath it. FIVE runs have been lost to this across
# three packages. And the trigger has THREE doors — this file, the workflow, and
# `scripts/data-contract/verification-sql.mjs`. A push carrying a migration must
# change NONE of them.
#
# READ THE REPORT, NOT THE EXIT CODE. The runner is tolerant: WP 3.4 shipped a
# probe that ordered by a column that does not exist, printed QUERY FAILED, and
# exited 0.
WP 4.3 analyzers, dual-write — BEFORE
#
# NO MIGRATION IS IN THIS PUSH, which is the whole point. This push changes two
# of the three doors (this file and the runner) and carries no migration, so
# nothing races a deploy. WP 4.1 and WP 4.2 each lost zero runs by sending their
# probes first; five were lost across the three packages before them.
#
# THREE THINGS NOTHING HAS EVER MEASURED, and every one is a number this package
# cannot take after it has acted:
#
#   1. `supply_chain_data` and `supply_chain_data_multi_tier` row counts, total
#      and PER PROJECT (D42). They are the two tier-3 tables in the contract
#      that gain `computed_from_hash` here and they are WP 5.3's before-number
#      as much as this package's. WP 4.1's probes did not touch them and WP
#      4.2's four derived tables are a different set, so "how many derived rows
#      carry no input hash" has only ever been answered for the network group.
#      `rows_with_input_hash` is a literal `0` and not a count, because there is
#      no column to count yet — that is the honest spelling of a before-figure
#      for a column that does not exist, and the after-run replaces the literal
#      with a real count.
#
#   2. `analysis_runs` BY KIND. WP 4.2 shipped with this at zero and said so in
#      three places. This package is the first real caller, so the after-run is
#      the first time a non-zero count means anything at all — and what it will
#      mean is **ANALYZERS RUN**, never adoption (§16 · WP 4.2 · N).
#
#   3. **THE GAP CHECK'S OWN COVERAGE.** §11 asks for a field-by-field numeric
#      comparison old-vs-new on a real project, and a comparison that silently
#      covers nothing is this plan's recurring failure mode. So the eight
#      computed field classes are counted SEPARATELY, per project: five
#      centralities, `prominence`, `is_critical_node`, `critical_node_score`.
#      The expectation, from WP 4.2's sweep, is that `network_nodes` metrics
#      exist in ONE project of ten (1 385 rows, 1 003 with a metrics timestamp)
#      while `node_list` spans seven projects and carries ZERO computed values —
#      which would mean two of the eight classes have no real data to compare
#      and the gap check must SAY so rather than reporting a clean comparison
#      over zero values. This run is what settles it.
#
# AND D72 IS RE-MEASURED, WITH ONE CLAIM CORRECTED BY ASKING THE DATABASE.
# D72's figure is four weeks stale the moment an analyzer runs, so the duplicate
# count is re-taken before any index is attempted. The correction is that D72's
# row asserts `uid` is NULLABLE and the creating migration declares
# `uid text NOT NULL`. D43's whole class is production disagreeing with the
# migrations, so the probe reads `attnotnull` from `pg_attribute` rather than
# trusting either document. The row's CONCLUSION survives either answer — the
# index is spelled `NULLS NOT DISTINCT` regardless, because nullability is a
# schema property a later `ALTER` can change — but whether that spelling is
# load-bearing today or defensive against a future `ALTER` is a different
# sentence, and only the database can say which one to write.
#
# Every query in this push was executed against a real PostgreSQL 16 holding the
# rehearsed schema, WITH ROWS, before being committed — including the seeded
# duplicate that exercises the "a dedup migration IS needed" branch. A probe
# that has not been run is a probe that measures nothing.
