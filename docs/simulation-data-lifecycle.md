# Simulation Project — Data Lifecycle — moved

> **Status: DEPRECATED** → superseded by **[`docs/data/lifecycle.md`](data/lifecycle.md)**.
> This path is a tombstone. It holds no content of its own; nothing here is
> maintained, and nothing should be added.

The state-tier reference now lives with the rest of the data-layer reference under
`docs/data/`, alongside [`field-mapping.md`](data/field-mapping.md) and the generated
table pages that land in WP 1.4.

This stub exists because other documents cite the old path; deleting it would turn
each citation into a dead reference. It is removed once those citations are updated,
which is WP 5.1's lineage work.

**The plan is `docs/PLAN.md`**, and its §4 is the only authority for data-layer
`file:line` evidence. `npm run check:docs` enforces that.
