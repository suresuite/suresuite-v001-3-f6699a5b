<!-- GENERATED by scripts/data-contract/generate.mjs from supabase/contract/ingest_staged_rows.contract.yaml + build/schema.introspected.json + the engine registry. Do not edit by hand: `npm run contract:check` fails on drift. -->

# `ingest_staged_rows`

> **GENERATED** — rendered from the data contract. Edit [`supabase/contract/ingest_staged_rows.contract.yaml`](../../../supabase/contract/ingest_staged_rows.contract.yaml)
> for what the columns mean, or the migrations for what the schema is. Do not edit
> this page: `npm run contract:check` fails when it differs from what the contract
> generates.

**Tier 1** — staging — parsed, diffed, unpromoted · owned by `data-ingestion` · `public.ingest_staged_rows`

**One row is** One row of one uploaded file, in one run: the cells as received beside what validation made of them, and the findings that explain the difference. The unit a reviewer reads when a number looks wrong — and the only place in the system where the left-hand side of a parse survives.

TIER 1, CREATED IN PHASE 3 / WP 3.2 — the fourth staging table and the last one of its kind. The three `ingest_staged_*` tables WP 3.1 renamed are TYPED against one source's entities (`external_id`, `sku`, `cycle_time_seconds`) because the connector knows what an orbit-mrp product is. A CSV of `inbound_logistics` is none of those things, and widening those three to hold it would have made each of them a table about two unrelated grains. So this one is generic where they are specific, and the generality is `ingestion-contract` (I7) working as intended: a new source lands here and never touches a tier-2 schema. WHAT IT CLOSES: the CSV path is the first in this repository for which `no-tier-skip` (I2) is true rather than aspirational. Before WP 3.2 a browser parsed the file and inserted straight into `inbound_logistics`.

## Uniqueness

| Columns | Source | Constraint |
|---|---|---|
| `ingest_run_id` + `target_table` + `source_row_number` | UNIQUE constraint | — |
| `id` | column PRIMARY KEY | `ingest_staged_rows_pkey` |

## Constraints

These reject the row outright. A value that fails one of them does not arrive
partially or get corrected — the write fails.

| Constraint | Rule | Added by |
|---|---|---|
| `ingest_staged_rows_source_kind_check` | `CHECK (source_kind IN ('csv', 'orbit-mrp', 'api'))` | `20260916000014_ingest_staged_rows.sql` |
| `ingest_staged_rows_fact_class_check` | `CHECK (fact_class IN ('master', 'transactional'))` | `20260916000014_ingest_staged_rows.sql` |
| `ingest_staged_rows_source_row_number_check` | `CHECK (source_row_number > 1)` | `20260916000014_ingest_staged_rows.sql` |
| `ingest_staged_rows_diff_state_check` | `CHECK (diff_state IN ('new', 'changed', 'unchanged', 'removed_upstream'))` | `20260916000014_ingest_staged_rows.sql` |

| Constraint | Kind | Definition |
|---|---|---|
| — | UNIQUE | `UNIQUE (ingest_run_id, target_table, source_row_number)` |

## Governance

| | |
|---|---|
| Read capability | any project member |
| Write capability | **no user-facing write path** |
| Minimum project role | `editor` |
| Tier transitions audited | yes |
| Row-level security | enabled |
| Policies on the table | 1 — all carry a predicate |

RLS reaches the project through the RUN — `has_project_access(r.project_id)` — never through `project_erp_links`, because a CSV run has no link and the old predicate could not reach it at all (WP 3.1). `authenticated` holds SELECT and nothing else: tier 1 is written by the landing function and read by the review screen, and a client that could INSERT here would be a client that could put a row into tier 1 without a file behind it. `audited: true` by the RPC route, not by a trigger: `ingest_land_file` writes one `plane='data'` audit row per landing, naming the uploader, and it can name them because the actor is a parameter rather than a session GUC (D36's failure mode, avoided rather than inherited). `supabase/rehearsal/070` asserts the row, the actor and the refusal.

<details><summary>1 RLS policy</summary>

| Policy | Command | Roles | Added by |
|---|---|---|---|
| ingest_staged_rows: project access | ALL | authenticated | `20260916000014_ingest_staged_rows.sql` |

</details>

## Where this data is read

| Page | Via | Evidence | Confirmed |
|---|---|---|---|
| `DataManager.tsx` | table read | `src/hooks/useIngestRun.tsx:63` | yes |

Each row says the page READS the table by that path, at that line. It does
not say every column below is displayed there — a column carries its own
lineage only where an explicit `select` names it. `npm run contract:check`
R12 re-opens every evidence line on each run, so an entry cannot go stale
unnoticed.

## Columns

`CSV header` is the name the **user types**, which is not always the column name —
that gap is defect D21. A dash means the column has no CSV origin.

| Column | CSV header | Type | Unit | Required in CSV | Meaning |
|---|---|---|---|---|---|
| `id` 🔑 | — | `uuid` | — | — | Surrogate key for one staged row. |
| `ingest_run_id` | — | `uuid` | — | — | The run these rows were staged in. NOT NULL, ON DELETE CASCADE: the run is the owner and the only route to a project, exactly as for `ingest_files` and the three connector staging tables. |
| `source_kind` | — | `text` | — | — | csv, orbit-mrp or api — the same closed vocabulary as the run's and the file's, CHECK-constrained with no DEFAULT. |
| `fact_class` | — | `text` | — | — | master or transactional — what KIND of fact one row is. Structure (a BOM) is master; a quantity over a period (a lane) is transactional. The value comes from the target table's own `ingest_dataset.fact_class`, so the contract decides it rather than the uploader. |
| `target_table` | — | `text` | — | — | The tier-2 table these rows are destined for, by name. Not a foreign key — there is no table of tables — so `ingest_target_is_promotable()` is the closed set, and the landing refuses anything outside it before a row is written. |
| `source_row_number` | — | `integer` | — | — | The physical line in the file, 1-based, counting the header as line 1 — so the first data row is 2, and the number matches what a spreadsheet shows the person who made the file. CHECK > 1 for that reason: line 1 is the header and can never be a staged row. |
| `raw` | — | `jsonb` | — | — | The cells exactly as the file gave them, keyed by the header the file carried — untrimmed, untyped, verbatim, including columns the contract does not know. The left-hand side of every parse claim. |
| `parsed` | — | `jsonb` | — | — | What validation made of the cells, keyed by TIER-2 COLUMN NAME rather than by CSV header — the two differ, and the mapping is the contract's `ingest.csv_header`. |
| `findings` | — | `jsonb` | — | — | [{level, field, code, message}] — the same four-key shape as `ingest_runs.mapping_warnings` and scsim's MappingWarning, so WP 3.4 renders it with `MappingWarningsCard` unmodified. |
| `diff_state` | — | `text` | — | — | What this row is relative to the CURRENT tier-2 table — new, changed or unchanged — computed by `ingest_diff_run` at landing, again when the review screen is opened and again inside `ingest_apply_run` immediately before the upsert. NULL means the diff has not been computed for this row: a row held back by an `error` finding may have failed on a key field and so has nothing to compare. The CHECK also admits `removed_upstream`, which is the same vocabulary the three connector staging tables use so that one review component serves both sources; nothing writing THIS table ever sets it. |
| `staged_at` | — | `timestamp with time zone` | — | — | When the row was staged. Server-stamped, never the client's clock. |

## Each column in full

### `id`

Surrogate key for one staged row.

| | |
|---|---|
| Type | `uuid`, `NOT NULL`, default `gen_random_uuid()` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20260916000014_ingest_staged_rows.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `ingest_run_id`

The run these rows were staged in. NOT NULL, ON DELETE CASCADE: the run is the owner and the only route to a project, exactly as for `ingest_files` and the three connector staging tables.

| | |
|---|---|
| Type | `uuid`, `NOT NULL` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20260916000014_ingest_staged_rows.sql` |
| References | `public.ingest_runs(id)` ON DELETE CASCADE |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | `[object Object]` |

**Rendered on** `DataManager.tsx` (`src/components/ingest/RowProvenance.tsx:51`) —
each of these names this column in an explicit `select` list, so the claim
is about the column and not only about the table.

### `source_kind`

csv, orbit-mrp or api — the same closed vocabulary as the run's and the file's, CHECK-constrained with no DEFAULT.

| | |
|---|---|
| Type | `text`, `NOT NULL` |
| Grain | `level` |
| Unit | dimensionless |
| Added by | `20260916000014_ingest_staged_rows.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | one of csv, orbit-mrp, api |
| Rendered at | *not yet recorded (WP 5.1)* |

> NOT AN ENGINE INPUT and never will be: tier 1 is invisible below it (I2). It is provenance, read by the review screen and by anyone asking where a tier-2 row came from.

### `fact_class`

master or transactional — what KIND of fact one row is. Structure (a BOM) is master; a quantity over a period (a lane) is transactional. The value comes from the target table's own `ingest_dataset.fact_class`, so the contract decides it rather than the uploader.

| | |
|---|---|
| Type | `text`, `NOT NULL` |
| Grain | `level` |
| Unit | dimensionless |
| Added by | `20260916000014_ingest_staged_rows.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | one of master, transactional |
| Rendered at | *not yet recorded (WP 5.1)* |

> NOT AN ENGINE INPUT, for the same reason `source_kind` is not. It exists so that a later package can treat master data and transactional data differently at promotion without re-deriving which is which from the target table's name.

### `target_table`

The tier-2 table these rows are destined for, by name. Not a foreign key — there is no table of tables — so `ingest_target_is_promotable()` is the closed set, and the landing refuses anything outside it before a row is written.

| | |
|---|---|
| Type | `text`, `NOT NULL` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20260916000014_ingest_staged_rows.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | must be a promotable target |
| Rendered at | *not yet recorded (WP 5.1)* |

### `source_row_number`

The physical line in the file, 1-based, counting the header as line 1 — so the first data row is 2, and the number matches what a spreadsheet shows the person who made the file. CHECK > 1 for that reason: line 1 is the header and can never be a staged row.

| | |
|---|---|
| Type | `integer`, `NOT NULL` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20260916000014_ingest_staged_rows.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | integer > 1 |
| Rendered at | `[object Object]` |

**Rendered on** `DataManager.tsx` (`src/components/ingest/RowProvenance.tsx:51`) —
each of these names this column in an explicit `select` list, so the claim
is about the column and not only about the table.

> This is the anchor WP 3.4's review screen clicks through to and WP 3.3's tier-2 `source_row_id` points at. It is also why a rejected row is KEPT rather than dropped: "row 42 is why" needs row 42 to exist somewhere.

### `raw`

The cells exactly as the file gave them, keyed by the header the file carried — untrimmed, untyped, verbatim, including columns the contract does not know. The left-hand side of every parse claim.

| | |
|---|---|
| Type | `jsonb`, `NOT NULL` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260916000014_ingest_staged_rows.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> Keeping it is what makes D8 and D46 diagnosable rather than arguable: `" MAT-1 "` and `"MAT-1"` resolve to one id in `parsed` and remain two different strings here, and a `time_unit` of `21` is visible as the token it was rather than as the weekly it used to become.

### `parsed`

What validation made of the cells, keyed by TIER-2 COLUMN NAME rather than by CSV header — the two differ, and the mapping is the contract's `ingest.csv_header`.

| | |
|---|---|
| Type | `jsonb`, `NOT NULL`, default `'{}'::jsonb` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260916000014_ingest_staged_rows.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> A field that failed validation is ABSENT here rather than present as null. The distinction is the whole of D7: absent says "no value was established", null says "the file said empty", and a validator that spells them the same way puts 376 null volumes into tier 2 without telling anyone.

### `findings`

[{level, field, code, message}] — the same four-key shape as `ingest_runs.mapping_warnings` and scsim's MappingWarning, so WP 3.4 renders it with `MappingWarningsCard` unmodified.

| | |
|---|---|
| Type | `jsonb`, `NOT NULL`, default `'[]'::jsonb` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260916000014_ingest_staged_rows.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> A row carrying any `error` finding is never promoted — that is what "rejected with a row-level finding" means, and it is enforced in `ingest_apply_run`'s WHERE clause rather than hoped for. `warn` and `info` rows ARE promoted and shown, which is §5 T2: a substitution is visible at the point of display, not withheld by rejecting the row it is about.

### `diff_state`

What this row is relative to the CURRENT tier-2 table — new, changed or unchanged — computed by `ingest_diff_run` at landing, again when the review screen is opened and again inside `ingest_apply_run` immediately before the upsert. NULL means the diff has not been computed for this row: a row held back by an `error` finding may have failed on a key field and so has nothing to compare. The CHECK also admits `removed_upstream`, which is the same vocabulary the three connector staging tables use so that one review component serves both sources; nothing writing THIS table ever sets it.

| | |
|---|---|
| Type | `text` |
| Grain | `level` |
| Unit | dimensionless |
| Added by | `20260916000014_ingest_staged_rows.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | one of new, changed, unchanged, removed_upstream |
| Rendered at | *not yet recorded (WP 5.1)* |

> NULL IS "NOT COMPUTED", AND IT REPLACED A DEFAULT THAT WAS AN ANSWER. Until WP 3.4 this column was `NOT NULL DEFAULT 'new'` and no code path wrote it — `20260916000019` does not mention it once — so every staged row claimed `new`, including the rows the upsert UPDATED, and a review screen reading it would have rendered "340 new" for 340 unchanged rows with a number and no hedge. PLAN.md §4 D62. A file run never writes `removed_upstream`: a connector PULL is a statement about the whole source and a FILE is not — nothing about one upload says it is the complete set of a project's rows, so an absent row means the file does not mention it, and `ingest_runs.rows_removed` is 0 for every file run.

### `staged_at`

When the row was staged. Server-stamped, never the client's clock.

| | |
|---|---|
| Type | `timestamp with time zone`, `NOT NULL`, default `now()` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260916000014_ingest_staged_rows.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

## Indexes

| Index | Columns | Unique | Added by |
|---|---|---|---|
| `ingest_staged_rows_run_idx` | `ingest_run_id`, `target_table`, `source_row_number` | no | `20260916000014_ingest_staged_rows.sql` |
| `ingest_staged_rows_rejected_idx` | `ingest_run_id` | no | `20260916000014_ingest_staged_rows.sql` |

---

*Generated from data contract `81c7c8c0e8bb`, engine `0.2.3`,
sidecar `supabase/contract/ingest_staged_rows.contract.yaml`, table created by `20260916000014_ingest_staged_rows.sql`. No wall-clock date: a generated
page that differs from itself tomorrow cannot be drift-gated.*
