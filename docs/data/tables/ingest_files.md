<!-- GENERATED by scripts/data-contract/generate.mjs from supabase/contract/ingest_files.contract.yaml + build/schema.introspected.json + the engine registry. Do not edit by hand: `npm run contract:check` fails on drift. -->

# `ingest_files`

> **GENERATED** — rendered from the data contract. Edit [`supabase/contract/ingest_files.contract.yaml`](../../../supabase/contract/ingest_files.contract.yaml)
> for what the columns mean, or the migrations for what the schema is. Do not edit
> this page: `npm run contract:check` fails when it differs from what the contract
> generates.

**Tier 0** — landing — raw bytes as received · owned by `data-ingestion` · `public.ingest_files`

**One row is** One file as received, in one run: the manifest for bytes held in storage — where they are, how many there were, and the SHA-256 of exactly the sequence received. Write-once: the row records an event that has already happened and cannot be edited into a different one.

TIER 0, CREATED IN PHASE 3 / WP 3.1, AND NOTHING WRITES IT YET. PLAN.md §2 lists six tiers and this repository had five: there has never been a record of what a user actually uploaded. `UploadWizard` parses the file in the browser and throws it away, which is why every parse defect — D6's quoted comma, D7's blank numerics, D8's untrimmed identifiers, D46's integer `time_unit` — is unreproducible after the fact. You cannot re-read a file that was never kept. WP 3.2 is the writer: upload lands the bytes here, opens a run, and parses SERVER-SIDE. Landing the table one package ahead means that package does not have to invent a schema and a parser in the same session. WRITE-ONCE IS ENFORCED, NOT ASSERTED. `ingest_files_write_once()` refuses every UPDATE, and `supabase/rehearsal/060` proves it against a real database — both halves, because the rule is 'immutable' and not 'undeletable'. §2's tier laws have been sentences in a plan for three phases; a sentence nothing enforces drifts within two months, which is the lesson of the two orphan tables.

## Uniqueness

| Columns | Source | Constraint |
|---|---|---|
| `storage_bucket` + `storage_path` | UNIQUE constraint | — |
| `id` | column PRIMARY KEY | `ingest_files_pkey` |

## Constraints

| Constraint | Kind | Definition |
|---|---|---|
| — | UNIQUE | `UNIQUE (storage_bucket, storage_path)` |

## Governance

| | |
|---|---|
| Read capability | any project member |
| Write capability | **no user-facing write path** |
| Minimum project role | `editor` |
| Tier transitions audited | **no** — invariant `audit-actor` is not met here yet |
| Row-level security | enabled |
| Policies on the table | 1 — all carry a predicate |

RLS reaches the project through the run, the same predicate the staging tables use. The grant is narrower than theirs on purpose — `authenticated` holds SELECT and INSERT and nothing else — because tier 0 is write-once and a grant that permits UPDATE would be a promise the trigger then breaks. The trigger is the backstop the grant cannot be: the service role holds every privilege by definition, and `ingest_files_write_once()` refuses its updates too. `audited: false`, and here it is the most pointed of the four: a landing IS a tier transition, external bytes entering the system, and G4 says a tier transition writes an audit row naming the actor. What this table does instead is name the actor IN THE ROW (`uploaded_by`), which is better than nothing and less than the invariant asks. It has an owner: WP 3.2 writes the landing path, and an audit row is cheap to add from the function that already knows who uploaded the file — which is exactly the knowledge the service-role writers of D36 do not have.

<details><summary>1 RLS policy</summary>

| Policy | Command | Roles | Added by |
|---|---|---|---|
| ingest_files: project access | ALL | authenticated | `20260916000013_ingest_files_tier0.sql` |

</details>

## Columns

`CSV header` is the name the **user types**, which is not always the column name —
that gap is defect D21. A dash means the column has no CSV origin.

| Column | CSV header | Type | Unit | Required in CSV | Meaning |
|---|---|---|---|---|---|
| `id` 🔑 | — | `uuid` | — | — | Surrogate key for one landed file. |
| `ingest_run_id` | — | `uuid` | — | — | The run this file was landed for. NOT NULL: a file with no run is a byte-stream nobody asked for, and making the run the owner is what keeps a landing attached to a project, a source kind and an approval. |
| `source_kind` | — | `text` | — | — | csv, orbit-mrp or api — what kind of source produced these bytes. The same closed vocabulary as the run's. |
| `original_filename` | — | `text` | — | — | The file's name as the user's machine gave it, verbatim. For display and for the person's own recognition — never a key, and never parsed for meaning: two projects upload `inbound.csv` on the same afternoon (G1's rule, applied to filenames). |
| `storage_bucket` | — | `text` | — | — | The storage bucket the bytes live in. Recorded rather than assumed, so that moving a bucket is a data change and not a code change. |
| `storage_path` | — | `text` | — | — | Where the bytes are inside the bucket. Generated by the writer, never supplied by the user, and UNIQUE with the bucket because it addresses exactly one stored object. |
| `content_type` | — | `text` | — | — | The MIME type as reported at upload. NULLABLE and not trusted: browsers report `application/vnd.ms-excel` for CSVs often enough that a parser keying on it would reject real files. WP 3.2's parser decides from the bytes. |
| `byte_size` | — | `bigint` | — | — | How many bytes were received. The cheapest possible check that a file is the file somebody meant to send, and the number a quota will eventually be about. |
| `content_sha256` | — | `text` | — | — | SHA-256 of the bytes AS RECEIVED, lowercase hex, computed before any parse. The anchor: with it, 'the file you uploaded on Tuesday' is a checkable claim rather than a memory. |
| `uploaded_by` | — | `uuid` | — | — | The person who uploaded the file, in `approved_users`. NULL when the landing had no human — an API push or a scheduled pull — which is stated rather than filled with a service account. |
| `received_at` | — | `timestamp with time zone` | — | — | When the bytes were received. Server-stamped, never the client's clock. |

## Each column in full

### `id`

Surrogate key for one landed file.

| | |
|---|---|
| Type | `uuid`, `NOT NULL`, default `gen_random_uuid()` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20260916000013_ingest_files_tier0.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `ingest_run_id`

The run this file was landed for. NOT NULL: a file with no run is a byte-stream nobody asked for, and making the run the owner is what keeps a landing attached to a project, a source kind and an approval.

| | |
|---|---|
| Type | `uuid`, `NOT NULL` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20260916000013_ingest_files_tier0.sql` |
| References | `ingest_runs(id)` ON DELETE CASCADE |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> ON DELETE CASCADE, and the direction matters: deleting a project deletes its runs and therefore its file manifests. Tier 0 is immutable, not immortal — a RESTRICT here would have made a project undeletable, which is a defect dressed as rigour.

### `source_kind`

csv, orbit-mrp or api — what kind of source produced these bytes. The same closed vocabulary as the run's.

| | |
|---|---|
| Type | `text`, `NOT NULL` |
| Grain | `level` |
| Unit | dimensionless |
| Added by | `20260916000013_ingest_files_tier0.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> No default here either, and unlike the other two tables this column never needed a backfill: the table is new and has no rows to be wrong about.

### `original_filename`

The file's name as the user's machine gave it, verbatim. For display and for the person's own recognition — never a key, and never parsed for meaning: two projects upload `inbound.csv` on the same afternoon (G1's rule, applied to filenames).

| | |
|---|---|
| Type | `text`, `NOT NULL` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260916000013_ingest_files_tier0.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `storage_bucket`

The storage bucket the bytes live in. Recorded rather than assumed, so that moving a bucket is a data change and not a code change.

| | |
|---|---|
| Type | `text`, `NOT NULL` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260916000013_ingest_files_tier0.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `storage_path`

Where the bytes are inside the bucket. Generated by the writer, never supplied by the user, and UNIQUE with the bucket because it addresses exactly one stored object.

| | |
|---|---|
| Type | `text`, `NOT NULL` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20260916000013_ingest_files_tier0.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `content_type`

The MIME type as reported at upload. NULLABLE and not trusted: browsers report `application/vnd.ms-excel` for CSVs often enough that a parser keying on it would reject real files. WP 3.2's parser decides from the bytes.

| | |
|---|---|
| Type | `text` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260916000013_ingest_files_tier0.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `byte_size`

How many bytes were received. The cheapest possible check that a file is the file somebody meant to send, and the number a quota will eventually be about.

| | |
|---|---|
| Type | `bigint`, `NOT NULL` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260916000013_ingest_files_tier0.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `content_sha256`

SHA-256 of the bytes AS RECEIVED, lowercase hex, computed before any parse. The anchor: with it, 'the file you uploaded on Tuesday' is a checkable claim rather than a memory.

| | |
|---|---|
| Type | `text`, `NOT NULL` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20260916000013_ingest_files_tier0.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> CHECK-constrained on SHAPE (`^[0-9a-f]{64}$`) rather than on content, because a truncated or upper-cased hash compares unequal to itself and would break every lineage claim built on it — quietly, and much later. It is also the dedup signal: 'you have uploaded these bytes before' is a question asked by hash, not by filename, and the index exists for it.

### `uploaded_by`

The person who uploaded the file, in `approved_users`. NULL when the landing had no human — an API push or a scheduled pull — which is stated rather than filled with a service account.

| | |
|---|---|
| Type | `uuid` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20260916000013_ingest_files_tier0.sql` |
| References | `approved_users(id)` ON DELETE SET NULL |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `received_at`

When the bytes were received. Server-stamped, never the client's clock.

| | |
|---|---|
| Type | `timestamp with time zone`, `NOT NULL`, default `now()` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260916000013_ingest_files_tier0.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

## Indexes

| Index | Columns | Unique | Added by |
|---|---|---|---|
| `ingest_files_run_idx` | `ingest_run_id` | no | `20260916000013_ingest_files_tier0.sql` |
| `ingest_files_hash_idx` | `content_sha256` | no | `20260916000013_ingest_files_tier0.sql` |

---

*Generated from data contract `64f3f2bdb4a2`, engine `0.2.3`,
sidecar `supabase/contract/ingest_files.contract.yaml`, table created by `20260916000013_ingest_files_tier0.sql`. No wall-clock date: a generated
page that differs from itself tomorrow cannot be drift-gated.*
