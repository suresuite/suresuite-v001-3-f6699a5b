<!-- GENERATED by scripts/data-contract/generate.mjs from supabase/contract/ingest_staged_products.contract.yaml + build/schema.introspected.json + the engine registry. Do not edit by hand: `npm run contract:check` fails on drift. -->

# `ingest_staged_products`

> **GENERATED** — rendered from the data contract. Edit [`supabase/contract/ingest_staged_products.contract.yaml`](../../../supabase/contract/ingest_staged_products.contract.yaml)
> for what the columns mean, or the migrations for what the schema is. Do not edit
> this page: `npm run contract:check` fails when it differs from what the contract
> generates.

**Tier 1** — staging — parsed, diffed, unpromoted · owned by `data-ingestion` · `public.ingest_staged_products`

**One row is** One item-master row as one source sent it, inside one run. The source's own shape plus provenance — not the project's shape, which it takes on at promotion.

RENAMED FROM `erp_staged_products` IN PHASE 3 / WP 3.1. Nothing here is visible to the engine or to the pages, by tier rule (I2, `no-tier-skip`): staged rows reach tier 2 only through an approved promotion, which is why every `consumed_by` is null and why that is a statement rather than a gap. The assertions that hold this table to it are `supabase/rehearsal/050` and `060`. WHAT IS NOT HERE. No CSV writer — WP 3.2 builds it, and `fact_class` and `source_kind` exist so that it has somewhere truthful to write. No natural key: see the `id` field. No audit: see governance.

## Uniqueness

| Columns | Source | Constraint |
|---|---|---|
| `id` | column PRIMARY KEY | `erp_staged_products_pkey` |

## Constraints

These reject the row outright. A value that fails one of them does not arrive
partially or get corrected — the write fails.

| Constraint | Rule | Added by |
|---|---|---|
| `ingest_staged_products_source_kind_check` | `CHECK (source_kind IN ('csv', 'orbit-mrp', 'api'))` | `20260916000012_ingest_rename_and_widen.sql` |
| `ingest_staged_products_fact_class_check` | `CHECK (fact_class IN ('master', 'transactional'))` | `20260916000012_ingest_rename_and_widen.sql` |

## Governance

| | |
|---|---|
| Read capability | any project member |
| Write capability | **no user-facing write path** |
| Minimum project role | `editor` |
| Tier transitions audited | **no** — invariant `audit-actor` is not met here yet |
| Row-level security | enabled |
| Policies on the table | 1 — all carry a predicate |

RLS reaches the project through the RUN — `EXISTS (SELECT 1 FROM ingest_runs r WHERE r.id = ingest_run_id AND has_project_access(r.project_id))`. WP 3.1 rewrote it: the original went through `project_erp_links`, which a run without a link cannot satisfy. Two things about this table were invisible to the contract until that rewrite, and both are worth recording. Its policy was created inside a `DO $$ EXECUTE format(...) $$` loop, which `introspect.mjs` does not read, so the contract recorded RLS enabled and ZERO policies and the generated page published that — D28's blind spot from the other side, hiding protection rather than exposure. The policies are now four literal statements. `audited: false`: no tier-2 transition happens here, and the writer is the service role (D36). The transition that DOES need an audit row is promotion, which WP 3.3 makes an audited upsert.

<details><summary>1 RLS policy</summary>

| Policy | Command | Roles | Added by |
|---|---|---|---|
| ingest_staged_products: project access | ALL | authenticated | `20260916000012_ingest_rename_and_widen.sql` |

</details>

## Columns

`CSV header` is the name the **user types**, which is not always the column name —
that gap is defect D21. A dash means the column has no CSV origin.

| Column | CSV header | Type | Unit | Required in CSV | Meaning |
|---|---|---|---|---|---|
| `id` 🔑 | — | `uuid` | — | — | Surrogate key for one staged row. The staging tables carry no natural key on purpose: they hold what a source SAID, including whatever duplicates it said it with, and deduplication is a decision made at promotion (WP 3.3), not a constraint that silently discards a source row before anybody has seen it. |
| `ingest_run_id` | — | `uuid` | — | — | The run that staged this row. The owner: staged rows cascade with their run, and the run is what carries the project, the source kind and the approval. |
| `link_id` | — | `uuid` | — | — | The ERP link the row came through, or NULL for a source that has none. Redundant with the run's own `link_id` and kept because the connector writes it; the RUN is the authority (I1) and a reader that disagrees should believe the run. |
| `external_id` | — | `text` | — | — | The row's identifier IN THE SOURCE, exactly as the source spells it. Never trimmed or case-folded here: tier 1 holds what arrived, and normalizing at this tier would destroy the evidence that a source sent `" MAT-1 "` (D8). |
| `sku` | — | `text` | — | — | The source's stock-keeping unit. Becomes `products.product_id` at promotion when present, which is why a source with no SKU falls back to `external_id` — stated here because a silent fallback is exactly what I6 forbids. |
| `name` | — | `text` | — | — | The item's display name in the source. Never a join key (G1). |
| `product_type` | — | `text` | — | — | finished_good, subassembly or raw_material as the SOURCE classifies it. Not CHECK-constrained: the vocabulary is the source's, and forcing it into ours at tier 1 would be normalizing before anybody has looked. |
| `unit_of_measure` | — | `text` | — | — | The unit the source quotes quantities in, in the source's own vocabulary (EA, KG, M). Recorded, NOT converted: `normalize-at-promotion` (I3) says units normalize on the way into tier 2 and nowhere else. |
| `lead_time_days` | — | `numeric` | `day` | — | The source's replenishment lead time for the item, in DAYS as the source states it. |
| `moq` | — | `numeric` | — | — | Minimum order quantity in the source's own unit of measure. |
| `unit_cost` | — | `numeric` | — | — | The source's unit cost, in the source's currency. No currency column exists — the connector never sent one — so a multi-currency source would need one before this value could be compared with anything. |
| `supplier_name` | — | `text` | — | — | The supplier the source names for this item, as free text. A NAME, and therefore never a join key (G1) — resolving it to a `suppliers` row is a decision the review screen will have to surface, not one to make silently. |
| `supplier_number` | — | `text` | — | — | The supplier's identifier in the source system. This is the one that can resolve to a supplier, and it still does not today. |
| `cycle_time_seconds` | — | `numeric` | `second` | — | Production cycle time per unit, in seconds, as the source states it. |
| `raw` | — | `jsonb` | — | — | The complete source row, verbatim. The reason a field the mapper does not yet understand is not lost: when a column turns out to matter, the answer for rows already staged is in here rather than in a re-sync. |
| `diff_state` | — | `text` | — | — | new, changed, unchanged or removed_upstream — what the sync decided this row is, relative to the live tier-2 table. `removed_upstream` is the one with teeth: it is NEVER promoted, because a source dropping a row is not authority to delete the project's row. |
| `staged_at` | — | `timestamp with time zone` | — | — | When the row landed in staging. Server-stamped, and it is what the promotion copies into `products.source_synced_at`, so a live row can say how old the source data behind it is. |
| `source_kind` | — | `text` | — | — | csv, orbit-mrp or api — where this row came from. CHECK-constrained, and it matches its run's value for every row any current writer produces. |
| `fact_class` | — | `text` | — | — | master or transactional. Master rows describe things that persist — an item, a BOM, a supplier; transactional rows describe events with a date. Nothing stages transactional rows yet; the vocabulary exists so WP 3.2's parser has one to declare against rather than inventing one per file. |

## Each column in full

### `id`

Surrogate key for one staged row. The staging tables carry no natural key on purpose: they hold what a source SAID, including whatever duplicates it said it with, and deduplication is a decision made at promotion (WP 3.3), not a constraint that silently discards a source row before anybody has seen it.

| | |
|---|---|
| Type | `uuid`, `NOT NULL`, default `gen_random_uuid()` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20260829120000_erp_connector_phase1_2.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `ingest_run_id`

The run that staged this row. The owner: staged rows cascade with their run, and the run is what carries the project, the source kind and the approval.

| | |
|---|---|
| Type | `uuid`, `NOT NULL` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20260829120000_erp_connector_phase1_2.sql` |
| References | `ingest_runs(id)` ON DELETE CASCADE |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> Renamed from `sync_run_id` in WP 3.1, with the table it points at. WP 3.3 puts the same column on the tier-2 lanes, so one word covers the whole path from a file to a canonical row.

### `link_id`

The ERP link the row came through, or NULL for a source that has none. Redundant with the run's own `link_id` and kept because the connector writes it; the RUN is the authority (I1) and a reader that disagrees should believe the run.

| | |
|---|---|
| Type | `uuid` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20260829120000_erp_connector_phase1_2.sql` |
| References | `project_erp_links(id)` ON DELETE CASCADE |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `external_id`

The row's identifier IN THE SOURCE, exactly as the source spells it. Never trimmed or case-folded here: tier 1 holds what arrived, and normalizing at this tier would destroy the evidence that a source sent `" MAT-1 "` (D8).

| | |
|---|---|
| Type | `text`, `NOT NULL` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20260829120000_erp_connector_phase1_2.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `sku`

The source's stock-keeping unit. Becomes `products.product_id` at promotion when present, which is why a source with no SKU falls back to `external_id` — stated here because a silent fallback is exactly what I6 forbids.

| | |
|---|---|
| Type | `text` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20260829120000_erp_connector_phase1_2.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `name`

The item's display name in the source. Never a join key (G1).

| | |
|---|---|
| Type | `text` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260829120000_erp_connector_phase1_2.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `product_type`

finished_good, subassembly or raw_material as the SOURCE classifies it. Not CHECK-constrained: the vocabulary is the source's, and forcing it into ours at tier 1 would be normalizing before anybody has looked.

| | |
|---|---|
| Type | `text` |
| Grain | `level` |
| Unit | dimensionless |
| Added by | `20260829120000_erp_connector_phase1_2.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> Not mapped into tier 2 by the connector's promotion today. It is kept because the classification decides which item master a row belongs in, and WP 3.4's review screen is where that decision gets a surface.

### `unit_of_measure`

The unit the source quotes quantities in, in the source's own vocabulary (EA, KG, M). Recorded, NOT converted: `normalize-at-promotion` (I3) says units normalize on the way into tier 2 and nowhere else.

| | |
|---|---|
| Type | `text` |
| Grain | `level` |
| Unit | dimensionless |
| Added by | `20260829120000_erp_connector_phase1_2.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> Nothing converts it yet. The promotion this sits behind writes only identity and name, so no quantity crosses the tier boundary and no conversion can be silently wrong. WP 3.3 is where the conversion lands, and this column is its input.

### `lead_time_days`

The source's replenishment lead time for the item, in DAYS as the source states it.

| | |
|---|---|
| Type | `numeric` |
| Grain | `level` |
| Unit | `day` — fixed |
| Added by | `20260829120000_erp_connector_phase1_2.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> DAYS here, weeks in `inbound_logistics.lead_time` (the unit table, `contract:units`). The conversion belongs at promotion (I3) and does not exist yet; nothing reads this column, so nothing converts it wrongly today.

### `moq`

Minimum order quantity in the source's own unit of measure.

| | |
|---|---|
| Type | `numeric` |
| Grain | `level` |
| Unit | dimensionless |
| Added by | `20260829120000_erp_connector_phase1_2.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> Untraced by design at this tier. `materials.moq` is the tier-2 column it would become; the connector's promotion does not write it yet.

### `unit_cost`

The source's unit cost, in the source's currency. No currency column exists — the connector never sent one — so a multi-currency source would need one before this value could be compared with anything.

| | |
|---|---|
| Type | `numeric` |
| Grain | `level` |
| Unit | dimensionless |
| Added by | `20260829120000_erp_connector_phase1_2.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> Recorded as a known limit rather than a silent one (§5 T3). Nothing reads it: promotion does not write cost, so no figure in the product depends on this number today.

### `supplier_name`

The supplier the source names for this item, as free text. A NAME, and therefore never a join key (G1) — resolving it to a `suppliers` row is a decision the review screen will have to surface, not one to make silently.

| | |
|---|---|
| Type | `text` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260829120000_erp_connector_phase1_2.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `supplier_number`

The supplier's identifier in the source system. This is the one that can resolve to a supplier, and it still does not today.

| | |
|---|---|
| Type | `text` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20260829120000_erp_connector_phase1_2.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `cycle_time_seconds`

Production cycle time per unit, in seconds, as the source states it.

| | |
|---|---|
| Type | `numeric` |
| Grain | `level` |
| Unit | `second` — fixed |
| Added by | `20260829120000_erp_connector_phase1_2.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> Seconds is the SOURCE's unit and the column name says so, which is the rule the unit table enforces for tier 2 (`contract:units`). Nothing reads it; the conversion to the engine's unit would happen at promotion (I3).

### `raw`

The complete source row, verbatim. The reason a field the mapper does not yet understand is not lost: when a column turns out to matter, the answer for rows already staged is in here rather than in a re-sync.

| | |
|---|---|
| Type | `jsonb`, `NOT NULL` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260829120000_erp_connector_phase1_2.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `diff_state`

new, changed, unchanged or removed_upstream — what the sync decided this row is, relative to the live tier-2 table. `removed_upstream` is the one with teeth: it is NEVER promoted, because a source dropping a row is not authority to delete the project's row.

| | |
|---|---|
| Type | `text`, `NOT NULL`, default `'new'` |
| Grain | `level` |
| Unit | dimensionless |
| Added by | `20260829120000_erp_connector_phase1_2.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> Computed by the connector as a full-refresh comparison against `products.source_external_id`. WP 3.4 renders it and WP 3.3 makes promotion an upsert keyed on the natural key, at which point `changed` and `new` stop being a guess about what the upsert will do.

### `staged_at`

When the row landed in staging. Server-stamped, and it is what the promotion copies into `products.source_synced_at`, so a live row can say how old the source data behind it is.

| | |
|---|---|
| Type | `timestamp with time zone`, `NOT NULL`, default `now()` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260829120000_erp_connector_phase1_2.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> Renamed from `synced_at` in WP 3.1: a CSV upload is not a sync, and the column had to mean the same thing for every source kind or it would have needed a second one.

### `source_kind`

csv, orbit-mrp or api — where this row came from. CHECK-constrained, and it matches its run's value for every row any current writer produces.

| | |
|---|---|
| Type | `text`, `NOT NULL` |
| Grain | `level` |
| Unit | dimensionless |
| Added by | `20260916000012_ingest_rename_and_widen.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> NO DEFAULT after the backfill: a writer states its source. Duplicated from the run deliberately — a staged row is read in bulk and joining to the run to learn what it is would make the common read the expensive one. If the two ever disagree the run wins.

### `fact_class`

master or transactional. Master rows describe things that persist — an item, a BOM, a supplier; transactional rows describe events with a date. Nothing stages transactional rows yet; the vocabulary exists so WP 3.2's parser has one to declare against rather than inventing one per file.

| | |
|---|---|
| Type | `text`, `NOT NULL` |
| Grain | `level` |
| Unit | dimensionless |
| Added by | `20260916000012_ingest_rename_and_widen.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> Every connector row is `master`, which is a fact about the connector and not a placeholder: it pulls products, BOM versions and BOM lines. The backfill therefore states something true, and then the DEFAULT is dropped so the next source has to answer the question itself.

## Indexes

| Index | Columns | Unique | Added by |
|---|---|---|---|
| `ingest_staged_products_run_idx` | `ingest_run_id` | no | `20260916000012_ingest_rename_and_widen.sql` |

---

*Generated from data contract `79191f91f602`, engine `0.2.3`,
sidecar `supabase/contract/ingest_staged_products.contract.yaml`, table created by `20260829120000_erp_connector_phase1_2.sql`. No wall-clock date: a generated
page that differs from itself tomorrow cannot be drift-gated.*
