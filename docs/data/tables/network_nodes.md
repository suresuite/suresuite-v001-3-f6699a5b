<!-- GENERATED by scripts/data-contract/generate.mjs from supabase/contract/network_nodes.contract.yaml + build/schema.introspected.json + the engine registry. Do not edit by hand: `npm run contract:check` fails on drift. -->

# `network_nodes`

> **GENERATED** — rendered from the data contract. Edit [`supabase/contract/network_nodes.contract.yaml`](../../../supabase/contract/network_nodes.contract.yaml)
> for what the columns mean, or the migrations for what the schema is. Do not edit
> this page: `npm run contract:check` fails when it differs from what the contract
> generates.

**Tier 3** — derived — a pure function of tier 2 · owned by `analysis` · `public.network_nodes`

**One row is** One firm in one project's deep-tier network graph, identified by `uid`. THE TABLE IS TWO THINGS AND THAT IS §4 D56: nine columns a user uploaded and eight an analysis wrote. WP 4.3 gives the computed half a second home in `analysis_results`; WP 5.3 drops it from here, and what is left is a tier-2 input table.

## Uniqueness

| Columns | Source | Constraint |
|---|---|---|
| `id` | column PRIMARY KEY | `network_nodes_pkey` |
| `project_id` + `uid` | UNIQUE index | `network_nodes_natural_key` |

**Natural key:** `project_id` + `uid` — the key this table's grain implies, and the
database ENFORCES it: `network_nodes_natural_key`. A re-upload of the same row updates
it rather than duplicating it.

## Governance

| | |
|---|---|
| Read capability | any project member |
| Write capability | **no user-facing write path** |
| Minimum project role | `viewer` |
| Tier transitions audited | yes |
| Row-level security | enabled |
| Policies on the table | 4 — all carry a predicate |

Read follows project reachability (`has_project_access`), uuid-only since WP 3.0. `write: null` is a STATEMENT and not an omission: there is no user-facing write policy. Uploads go through `bulk_insert_network_nodes`, which takes the actor and sets `app.current_user_id` through `set_current_user_context`; the analysis half goes through `analysis_apply_node_metrics` (WP 4.3), which takes the actor and stamps the run's `input_hash`. `audited: true` as of WP 4.3 — the three `audit_tier_write` triggers land in `20260917000007`, and until then this table was deferred in `coverage.yaml`, which is to say OUTSIDE the audit rule with nothing to notice (D54).

<details><summary>4 RLS policies</summary>

| Policy | Command | Roles | Added by |
|---|---|---|---|
| Network nodes: project access delete | DELETE | all | `20260915000004_org_identity_dual_read.sql` |
| Network nodes: project access insert | INSERT | all | `20260915000004_org_identity_dual_read.sql` |
| Network nodes: project access update | UPDATE | all | `20260915000004_org_identity_dual_read.sql` |
| Network nodes: project access view | SELECT | all | `20260915000004_org_identity_dual_read.sql` |

</details>

## Where this data is read

| Page | Via | Evidence | Confirmed |
|---|---|---|---|
| `DataManager.tsx` | rpc get_project_dataset_status | `src/pages/DataManager.tsx:429` | yes |
| `ProductLevelNetwork.tsx` | rpc project_freshness | `src/pages/ProductLevelNetwork.tsx:227` | yes |
| `SimulationLab.tsx` | rpc project_freshness | `src/components/trust/useProjectFreshness.ts:24` | yes |

Each row says the page READS the table by that path, at that line. It does
not say every column below is displayed there — a column carries its own
lineage only where an explicit `select` names it. `npm run contract:check`
R12 re-opens every evidence line on each run, so an entry cannot go stale
unnoticed.

## Columns

`CSV header` is the name the **user types**, which is not always the column name —
that gap is defect D21. A dash means the column has no CSV origin.

| Column | CSV header | Type | Unit | Required in CSV | Meaning |
|---|---|---|---|---|---|
| `id` 🔑 | — | `uuid` | — | — | Surrogate row identifier. Carries no meaning: what identifies a row here is the natural key, not this. |
| `project_id` | — | `uuid` | — | — | The project this row belongs to. Referenced by uuid and never by a displayable name (`uuid-identity`, G1); cascades on project delete. |
| `plant_name` | — | `text` | — | — | The plant label the row was uploaded under. NOT a project scope — a plant name is not unique across projects, which is the ambiguity `analysis_mark_critical_nodes` had to work around by deriving the project from the rows it was given. |
| `organization` | — | `text` | — | — | Legacy organization display string, defaulted to `default_org`. A DISPLAY name and never a join key (`uuid-identity`, G1): the live predicate is `org_is_current_user_org`, uuid-only since WP 3.0 (D29). |
| `created_by` | — | `uuid` | — | — | The user who created the row, as the uploader asserted it. This application authenticates against `approved_users` rather than Supabase Auth, so the id is CLIENT-ASSERTED — a real constraint, not proof of identity (D28). |
| `uploaded_by` | — | `uuid` | — | — | The user the upload named. Same client-asserted caveat as `created_by` (D28). |
| `created_at` | — | `timestamp with time zone` | — | — | Row insert time, maintained by the database. |
| `updated_at` | — | `timestamp with time zone` | — | — | Row update time, maintained by the database. When a row was TOUCHED is not what it SAYS, which is why `input-hash` (I5) is anchored on `computed_from_hash` and not on this (§4 D12). |
| `uid` | — | `text` | — | — | The node's identifier within the project's graph, as the uploaded CSV gave it. It is the natural key with `project_id`, and `network_nodes_natural_key` has enforced that since WP 4.3 — before which there was NO unique index behind it and `calculate- network-science-metrics`'s fallback upsert named a conflict target PostgreSQL rejected with 42P10 on every run (§4 D72). NULLS NOT DISTINCT, because `uid` is nullable and a plain index would constrain every row except the null ones (D5). |
| `depth` | — | `integer` | — | — | Tier distance from the seed firm: 0 is the seed, 1 its direct partners, and so on. Uploaded, not computed. |
| `name` | — | `text` | — | — | Display name of the firm. Cosmetic — a rename must never invalidate a run, which is why `graphHashCoverage.test.ts` excludes `name` from the anchor. |
| `country` | — | `text` | — | — | Country of the firm, as uploaded. Joined to `risk_data` by country for the network risk views. |
| `industry` | — | `text` | — | — | Industry label of the firm, as uploaded. |
| `website` | — | `text` | — | — | Firm website, as uploaded. Display only. |
| `traded_as` | — | `text` | — | — | Ticker or trading symbol, as uploaded. Display only. |
| `number_of_employees` | — | `integer` | — | — | Headcount, as uploaded. Read by no analysis and by no engine mapping today — same class as D18, and a candidate for the WP 6.2 sweep. |
| `revenue` | — | `numeric` | — | — | Firm revenue, as uploaded. READ BY THE PROMINENCE ANALYSIS (`get_network_nodes_for_prominence` returns it), so it is one of the six columns `network_topology_hash` digests. |
| `lat` | — | `numeric` | — | — | Latitude, as uploaded. Display only — the map reads `node_list`, not this table. |
| `long` | — | `numeric` | — | — | Longitude, as uploaded. Display only. |
| `is_seed` | — | `boolean` | — | — | True for the focal firm the deep-tier crawl started from. |
| `prominence` | — | `numeric` | — | — | ANALYSIS OUTPUT — `calculate-node-prominence`'s composite score. Dual-written into `analysis_results` since WP 4.3, so it can be read with the run, the input hash and the code version that produced it. WP 5.3 drops the column once every reader has moved. |
| `prominence_updated_at` | — | `timestamp with time zone` | — | — | When `prominence` was last written. Superseded by the run: `computed_at` and `computed_from_hash` say the same thing and more. Dropped with the column in WP 5.3. |
| `degree_centrality` | — | `numeric` | — | — | ANALYSIS OUTPUT — `calculate-network-science-metrics`. Dual- written into `analysis_results` since WP 4.3. |
| `weighted_degree_centrality` | — | `numeric` | — | — | ANALYSIS OUTPUT — degree weighted by `relative_revenue`. Dual- written since WP 4.3. |
| `eigenvector_centrality` | — | `numeric` | — | — | ANALYSIS OUTPUT — dual-written since WP 4.3. |
| `betweenness_centrality` | — | `numeric` | — | — | ANALYSIS OUTPUT — dual-written since WP 4.3. |
| `closeness_centrality` | — | `numeric` | — | — | ANALYSIS OUTPUT — dual-written since WP 4.3. |
| `network_metrics_updated_at` | — | `timestamp with time zone` | — | — | When the five centralities were last written. Superseded by `computed_at`; dropped with the columns in WP 5.3. |
| `computed_from_hash` | — | `text` | — | — | WP 4.3 · the `analysis_runs.input_hash` of the run that wrote this row's computed columns — the world the number came from. NULL means "written before WP 4.3, provenance unknown", which is a reportable state and not a silent default (`declared-fallback`, I6). This is the column invariant `input-hash` (I5) is about. |
| `computed_at` | — | `timestamp with time zone` | — | — | WP 4.3 · when the run that wrote the computed columns finished. It is provenance, not staleness: staleness is `computed_from_hash <> current_graph_hash()`, which is WP 4.4's one rule, and a timestamp comparison is the thing that rule replaces (§4 D12). |

## Each column in full

### `id`

Surrogate row identifier. Carries no meaning: what identifies a row here is the natural key, not this.

| | |
|---|---|
| Type | `uuid`, `NOT NULL`, default `gen_random_uuid()` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20250904105527_e00a21a9-0120-48a7-a62d-e2db83fd4cc6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> The engine reads the tier-2 dataset through `datamap.py` and never the deep-tier graph, so no column of this table reaches it. The two centrality ANALYZERS do read some of them — `meaning` says which.

### `project_id`

The project this row belongs to. Referenced by uuid and never by a displayable name (`uuid-identity`, G1); cascades on project delete.

| | |
|---|---|
| Type | `uuid`, `NOT NULL` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20250904105527_e00a21a9-0120-48a7-a62d-e2db83fd4cc6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> The engine reads the tier-2 dataset through `datamap.py` and never the deep-tier graph, so no column of this table reaches it. The two centrality ANALYZERS do read some of them — `meaning` says which.

### `plant_name`

The plant label the row was uploaded under. NOT a project scope — a plant name is not unique across projects, which is the ambiguity `analysis_mark_critical_nodes` had to work around by deriving the project from the rows it was given.

| | |
|---|---|
| Type | `text`, `NOT NULL` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20250904105527_e00a21a9-0120-48a7-a62d-e2db83fd4cc6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> The engine reads the tier-2 dataset through `datamap.py` and never the deep-tier graph, so no column of this table reaches it. The two centrality ANALYZERS do read some of them — `meaning` says which.

### `organization`

Legacy organization display string, defaulted to `default_org`. A DISPLAY name and never a join key (`uuid-identity`, G1): the live predicate is `org_is_current_user_org`, uuid-only since WP 3.0 (D29).

| | |
|---|---|
| Type | `text`, `NOT NULL`, default `'default_org'` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20250904105527_e00a21a9-0120-48a7-a62d-e2db83fd4cc6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> The engine reads the tier-2 dataset through `datamap.py` and never the deep-tier graph, so no column of this table reaches it. The two centrality ANALYZERS do read some of them — `meaning` says which.

### `created_by`

The user who created the row, as the uploader asserted it. This application authenticates against `approved_users` rather than Supabase Auth, so the id is CLIENT-ASSERTED — a real constraint, not proof of identity (D28).

| | |
|---|---|
| Type | `uuid` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20250904105527_e00a21a9-0120-48a7-a62d-e2db83fd4cc6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> The engine reads the tier-2 dataset through `datamap.py` and never the deep-tier graph, so no column of this table reaches it. The two centrality ANALYZERS do read some of them — `meaning` says which.

### `uploaded_by`

The user the upload named. Same client-asserted caveat as `created_by` (D28).

| | |
|---|---|
| Type | `uuid` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20250904105527_e00a21a9-0120-48a7-a62d-e2db83fd4cc6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> The engine reads the tier-2 dataset through `datamap.py` and never the deep-tier graph, so no column of this table reaches it. The two centrality ANALYZERS do read some of them — `meaning` says which.

### `created_at`

Row insert time, maintained by the database.

| | |
|---|---|
| Type | `timestamp with time zone`, `NOT NULL`, default `now()` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20250904105527_e00a21a9-0120-48a7-a62d-e2db83fd4cc6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> The engine reads the tier-2 dataset through `datamap.py` and never the deep-tier graph, so no column of this table reaches it. The two centrality ANALYZERS do read some of them — `meaning` says which.

### `updated_at`

Row update time, maintained by the database. When a row was TOUCHED is not what it SAYS, which is why `input-hash` (I5) is anchored on `computed_from_hash` and not on this (§4 D12).

| | |
|---|---|
| Type | `timestamp with time zone`, `NOT NULL`, default `now()` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20250904105527_e00a21a9-0120-48a7-a62d-e2db83fd4cc6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> The engine reads the tier-2 dataset through `datamap.py` and never the deep-tier graph, so no column of this table reaches it. The two centrality ANALYZERS do read some of them — `meaning` says which.

### `uid`

The node's identifier within the project's graph, as the uploaded CSV gave it. It is the natural key with `project_id`, and `network_nodes_natural_key` has enforced that since WP 4.3 — before which there was NO unique index behind it and `calculate- network-science-metrics`'s fallback upsert named a conflict target PostgreSQL rejected with 42P10 on every run (§4 D72). NULLS NOT DISTINCT, because `uid` is nullable and a plain index would constrain every row except the null ones (D5).

| | |
|---|---|
| Type | `text`, `NOT NULL` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20250904105527_e00a21a9-0120-48a7-a62d-e2db83fd4cc6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> The engine reads the tier-2 dataset through `datamap.py` and never the deep-tier graph, so no column of this table reaches it. The two centrality ANALYZERS do read some of them — `meaning` says which.

### `depth`

Tier distance from the seed firm: 0 is the seed, 1 its direct partners, and so on. Uploaded, not computed.

| | |
|---|---|
| Type | `integer` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20250904105527_e00a21a9-0120-48a7-a62d-e2db83fd4cc6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> The engine reads the tier-2 dataset through `datamap.py` and never the deep-tier graph, so no column of this table reaches it. The two centrality ANALYZERS do read some of them — `meaning` says which.

### `name`

Display name of the firm. Cosmetic — a rename must never invalidate a run, which is why `graphHashCoverage.test.ts` excludes `name` from the anchor.

| | |
|---|---|
| Type | `text` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20250904105527_e00a21a9-0120-48a7-a62d-e2db83fd4cc6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> The engine reads the tier-2 dataset through `datamap.py` and never the deep-tier graph, so no column of this table reaches it. The two centrality ANALYZERS do read some of them — `meaning` says which.

### `country`

Country of the firm, as uploaded. Joined to `risk_data` by country for the network risk views.

| | |
|---|---|
| Type | `text` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20250904105527_e00a21a9-0120-48a7-a62d-e2db83fd4cc6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> The engine reads the tier-2 dataset through `datamap.py` and never the deep-tier graph, so no column of this table reaches it. The two centrality ANALYZERS do read some of them — `meaning` says which.

### `industry`

Industry label of the firm, as uploaded.

| | |
|---|---|
| Type | `text` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20250904105527_e00a21a9-0120-48a7-a62d-e2db83fd4cc6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> The engine reads the tier-2 dataset through `datamap.py` and never the deep-tier graph, so no column of this table reaches it. The two centrality ANALYZERS do read some of them — `meaning` says which.

### `website`

Firm website, as uploaded. Display only.

| | |
|---|---|
| Type | `text` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20250904105527_e00a21a9-0120-48a7-a62d-e2db83fd4cc6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> The engine reads the tier-2 dataset through `datamap.py` and never the deep-tier graph, so no column of this table reaches it. The two centrality ANALYZERS do read some of them — `meaning` says which.

### `traded_as`

Ticker or trading symbol, as uploaded. Display only.

| | |
|---|---|
| Type | `text` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20250904105527_e00a21a9-0120-48a7-a62d-e2db83fd4cc6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> The engine reads the tier-2 dataset through `datamap.py` and never the deep-tier graph, so no column of this table reaches it. The two centrality ANALYZERS do read some of them — `meaning` says which.

### `number_of_employees`

Headcount, as uploaded. Read by no analysis and by no engine mapping today — same class as D18, and a candidate for the WP 6.2 sweep.

| | |
|---|---|
| Type | `integer` |
| Grain | `level` |
| Unit | dimensionless |
| Added by | `20250904105527_e00a21a9-0120-48a7-a62d-e2db83fd4cc6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> The engine reads the tier-2 dataset through `datamap.py` and never the deep-tier graph, so no column of this table reaches it. The two centrality ANALYZERS do read some of them — `meaning` says which.

### `revenue`

Firm revenue, as uploaded. READ BY THE PROMINENCE ANALYSIS (`get_network_nodes_for_prominence` returns it), so it is one of the six columns `network_topology_hash` digests.

| | |
|---|---|
| Type | `numeric` |
| Grain | `level` |
| Unit | dimensionless |
| Added by | `20250904105527_e00a21a9-0120-48a7-a62d-e2db83fd4cc6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> The engine reads the tier-2 dataset through `datamap.py` and never the deep-tier graph, so no column of this table reaches it. The two centrality ANALYZERS do read some of them — `meaning` says which.

### `lat`

Latitude, as uploaded. Display only — the map reads `node_list`, not this table.

| | |
|---|---|
| Type | `numeric` |
| Grain | `level` |
| Unit | dimensionless |
| Added by | `20250904105527_e00a21a9-0120-48a7-a62d-e2db83fd4cc6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> The engine reads the tier-2 dataset through `datamap.py` and never the deep-tier graph, so no column of this table reaches it. The two centrality ANALYZERS do read some of them — `meaning` says which.

### `long`

Longitude, as uploaded. Display only.

| | |
|---|---|
| Type | `numeric` |
| Grain | `level` |
| Unit | dimensionless |
| Added by | `20250904105527_e00a21a9-0120-48a7-a62d-e2db83fd4cc6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> The engine reads the tier-2 dataset through `datamap.py` and never the deep-tier graph, so no column of this table reaches it. The two centrality ANALYZERS do read some of them — `meaning` says which.

### `is_seed`

True for the focal firm the deep-tier crawl started from.

| | |
|---|---|
| Type | `boolean`, default `false` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20250904105527_e00a21a9-0120-48a7-a62d-e2db83fd4cc6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> The engine reads the tier-2 dataset through `datamap.py` and never the deep-tier graph, so no column of this table reaches it. The two centrality ANALYZERS do read some of them — `meaning` says which.

### `prominence`

ANALYSIS OUTPUT — `calculate-node-prominence`'s composite score. Dual-written into `analysis_results` since WP 4.3, so it can be read with the run, the input hash and the code version that produced it. WP 5.3 drops the column once every reader has moved.

| | |
|---|---|
| Type | `numeric` |
| Grain | `level` |
| Unit | dimensionless |
| Added by | `20250905140016_b6e8e382-459a-4cbc-be12-cd46c578a61d.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> Analyzer output. Not an engine input — and deliberately outside `graph_hash` too, since folding a result into the identity of its own inputs is what `input-hash` (I5) exists to prevent.

### `prominence_updated_at`

When `prominence` was last written. Superseded by the run: `computed_at` and `computed_from_hash` say the same thing and more. Dropped with the column in WP 5.3.

| | |
|---|---|
| Type | `timestamp with time zone` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20250905140016_b6e8e382-459a-4cbc-be12-cd46c578a61d.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> The engine reads the tier-2 dataset through `datamap.py` and never the deep-tier graph, so no column of this table reaches it. The two centrality ANALYZERS do read some of them — `meaning` says which.

### `degree_centrality`

ANALYSIS OUTPUT — `calculate-network-science-metrics`. Dual- written into `analysis_results` since WP 4.3.

| | |
|---|---|
| Type | `numeric` |
| Grain | `level` |
| Unit | dimensionless |
| Added by | `20250925154019_c846dd00-c018-407f-8aa6-f6ce2915f6f6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> Analyzer output. Not an engine input — and deliberately outside `graph_hash` too, since folding a result into the identity of its own inputs is what `input-hash` (I5) exists to prevent.

### `weighted_degree_centrality`

ANALYSIS OUTPUT — degree weighted by `relative_revenue`. Dual- written since WP 4.3.

| | |
|---|---|
| Type | `numeric` |
| Grain | `level` |
| Unit | dimensionless |
| Added by | `20250925154019_c846dd00-c018-407f-8aa6-f6ce2915f6f6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> Analyzer output. Not an engine input — and deliberately outside `graph_hash` too, since folding a result into the identity of its own inputs is what `input-hash` (I5) exists to prevent.

### `eigenvector_centrality`

ANALYSIS OUTPUT — dual-written since WP 4.3.

| | |
|---|---|
| Type | `numeric` |
| Grain | `level` |
| Unit | dimensionless |
| Added by | `20250925154019_c846dd00-c018-407f-8aa6-f6ce2915f6f6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> Analyzer output. Not an engine input — and deliberately outside `graph_hash` too, since folding a result into the identity of its own inputs is what `input-hash` (I5) exists to prevent.

### `betweenness_centrality`

ANALYSIS OUTPUT — dual-written since WP 4.3.

| | |
|---|---|
| Type | `numeric` |
| Grain | `level` |
| Unit | dimensionless |
| Added by | `20250925154019_c846dd00-c018-407f-8aa6-f6ce2915f6f6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> Analyzer output. Not an engine input — and deliberately outside `graph_hash` too, since folding a result into the identity of its own inputs is what `input-hash` (I5) exists to prevent.

### `closeness_centrality`

ANALYSIS OUTPUT — dual-written since WP 4.3.

| | |
|---|---|
| Type | `numeric` |
| Grain | `level` |
| Unit | dimensionless |
| Added by | `20250925154019_c846dd00-c018-407f-8aa6-f6ce2915f6f6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> Analyzer output. Not an engine input — and deliberately outside `graph_hash` too, since folding a result into the identity of its own inputs is what `input-hash` (I5) exists to prevent.

### `network_metrics_updated_at`

When the five centralities were last written. Superseded by `computed_at`; dropped with the columns in WP 5.3.

| | |
|---|---|
| Type | `timestamp with time zone` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20250925154019_c846dd00-c018-407f-8aa6-f6ce2915f6f6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> The engine reads the tier-2 dataset through `datamap.py` and never the deep-tier graph, so no column of this table reaches it. The two centrality ANALYZERS do read some of them — `meaning` says which.

### `computed_from_hash`

WP 4.3 · the `analysis_runs.input_hash` of the run that wrote this row's computed columns — the world the number came from. NULL means "written before WP 4.3, provenance unknown", which is a reportable state and not a silent default (`declared-fallback`, I6). This is the column invariant `input-hash` (I5) is about.

| | |
|---|---|
| Type | `text` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260917000007_analyzer_provenance.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> Provenance. Not an engine input; it describes where a number came from.

### `computed_at`

WP 4.3 · when the run that wrote the computed columns finished. It is provenance, not staleness: staleness is `computed_from_hash <> current_graph_hash()`, which is WP 4.4's one rule, and a timestamp comparison is the thing that rule replaces (§4 D12).

| | |
|---|---|
| Type | `timestamp with time zone` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260917000007_analyzer_provenance.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> Provenance. Not an engine input; it describes where a number came from.

## Indexes

| Index | Columns | Unique | Added by |
|---|---|---|---|
| `idx_network_nodes_prominence` | `prominence` | no | `20250905140016_b6e8e382-459a-4cbc-be12-cd46c578a61d.sql` |
| `idx_network_nodes_project_id` | `project_id` | no | `20250908075907_36523d34-2b67-4f34-a20b-7076c1698395.sql` |
| `idx_network_nodes_project` | `project_id` | no | `20260712110000_network_metrics_check_performance.sql` |
| `network_nodes_natural_key` | `project_id`, `uid` | yes | `20260917000007_analyzer_provenance.sql` |

---

*Generated from data contract `3caef13024f9`, engine `0.2.3`,
sidecar `supabase/contract/network_nodes.contract.yaml`, table created by `20250904105527_e00a21a9-0120-48a7-a62d-e2db83fd4cc6.sql`. No wall-clock date: a generated
page that differs from itself tomorrow cannot be drift-gated.*
