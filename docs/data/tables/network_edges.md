<!-- GENERATED by scripts/data-contract/generate.mjs from supabase/contract/network_edges.contract.yaml + build/schema.introspected.json + the engine registry. Do not edit by hand: `npm run contract:check` fails on drift. -->

# `network_edges`

> **GENERATED** — rendered from the data contract. Edit [`supabase/contract/network_edges.contract.yaml`](../../../supabase/contract/network_edges.contract.yaml)
> for what the columns mean, or the migrations for what the schema is. Do not edit
> this page: `npm run contract:check` fails when it differs from what the contract
> generates.

**Tier 3** — derived — a pure function of tier 2 · owned by `analysis` · `public.network_edges`

**One row is** One directed relationship between two firms in one project's deep-tier graph. EVERY column is uploaded — nothing computes this table, which is why it gains no `computed_from_hash` in WP 4.3 although it sits in `graphHashCoverage.test.ts`'s DERIVED_AND_OUT list by name.

## Uniqueness

| Columns | Source | Constraint |
|---|---|---|
| `id` | column PRIMARY KEY | `network_edges_pkey` |

## Governance

| | |
|---|---|
| Read capability | any project member |
| Write capability | **no user-facing write path** |
| Minimum project role | `viewer` |
| Tier transitions audited | yes |
| Row-level security | enabled |
| Policies on the table | 4 — all carry a predicate |

Read follows project reachability. No user-facing write policy; uploads go through `bulk_insert_network_edges`, which takes the actor. `audited: true` as of WP 4.3 (`20260917000007`); it was deferred and therefore unaudited before that (D54).

<details><summary>4 RLS policies</summary>

| Policy | Command | Roles | Added by |
|---|---|---|---|
| Network edges: project access delete | DELETE | all | `20260915000004_org_identity_dual_read.sql` |
| Network edges: project access insert | INSERT | all | `20260915000004_org_identity_dual_read.sql` |
| Network edges: project access update | UPDATE | all | `20260915000004_org_identity_dual_read.sql` |
| Network edges: project access view | SELECT | all | `20260915000004_org_identity_dual_read.sql` |

</details>

## Where this data is read

| Page | Via | Evidence | Confirmed |
|---|---|---|---|
| `DataManager.tsx` | rpc get_project_dataset_status | `src/pages/DataManager.tsx:429` | yes |
| `ProductLevelNetwork.tsx` | rpc get_network_metrics_for_materials | `src/pages/ProductLevelNetwork.tsx:251` | yes |

Each row says the page READS the table by that path, at that line. It does
not say every column below is displayed there — a column carries its own
lineage only where an explicit `select` names it. `npm run contract:check`
R12 re-opens every evidence line on each run, so an entry cannot go stale
unnoticed.

## Columns

`CSV header` is the name the **user types**, which is not always the column name —
that gap is defect D21. A dash means the column has no CSV origin.

| Column | CSV header | Type | Unit | Required in CSV | Meaning |
|---|---|---|---|---|---|
| `id` 🔑 | — | `uuid` | — | — | Surrogate row identifier. Carries no meaning: what identifies a row here is the natural key, not this. |
| `project_id` | — | `uuid` | — | — | The project this row belongs to. Referenced by uuid and never by a displayable name (`uuid-identity`, G1); cascades on project delete. |
| `plant_name` | — | `text` | — | — | The plant label the row was uploaded under. NOT a project scope — a plant name is not unique across projects, which is the ambiguity `analysis_mark_critical_nodes` had to work around by deriving the project from the rows it was given. |
| `organization` | — | `text` | — | — | Legacy organization display string, defaulted to `default_org`. A DISPLAY name and never a join key (`uuid-identity`, G1): the live predicate is `org_is_current_user_org`, uuid-only since WP 3.0 (D29). |
| `created_by` | — | `uuid` | — | — | The user who created the row, as the uploader asserted it. This application authenticates against `approved_users` rather than Supabase Auth, so the id is CLIENT-ASSERTED — a real constraint, not proof of identity (D28). |
| `uploaded_by` | — | `uuid` | — | — | The user the upload named. Same client-asserted caveat as `created_by` (D28). |
| `created_at` | — | `timestamp with time zone` | — | — | Row insert time, maintained by the database. |
| `updated_at` | — | `timestamp with time zone` | — | — | Row update time, maintained by the database. When a row was TOUCHED is not what it SAYS, which is why `input-hash` (I5) is anchored on `computed_from_hash` and not on this (§4 D12). |
| `src_uid` | — | `text` | — | — | Source node `uid`. Part of the natural key with `project_id` and `dst_uid`; READ BY THE PROMINENCE ANALYSIS, so it is digested by `network_topology_hash`. |
| `dst_uid` | — | `text` | — | — | Destination node `uid`. Part of the natural key; digested by `network_topology_hash`. |
| `relation_type` | — | `text` | — | — | What the edge represents (supplier, customer, …), as uploaded. |
| `relative_revenue` | — | `numeric` | — | — | The share of revenue this relationship carries, as uploaded. READ BY THE PROMINENCE ANALYSIS as the edge weight, so it is digested by `network_topology_hash` — changing it changes every centrality in the project. |
| `relative_revenue_percentage` | — | `numeric` | — | — | The same share expressed as a percentage. Uploaded, and read by no analysis — a second spelling of one fact, which is the shape `single-source` (I1) exists to refuse. Named for the WP 6.2 sweep. |
| `depth` | — | `integer` | — | — | Tier distance of the edge from the seed, as uploaded. |
| `direction` | — | `text` | — | — | Upstream or downstream relative to the seed, as uploaded. |

## Each column in full

### `id`

Surrogate row identifier. Carries no meaning: what identifies a row here is the natural key, not this.

| | |
|---|---|
| Type | `uuid`, `NOT NULL`, default `gen_random_uuid()` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20250904105527_e00a21a9-0120-48a7-a62d-e2db83fd4cc6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> The engine reads the tier-2 dataset through `datamap.py` and never the deep-tier graph, so no column of this table reaches it. The two centrality ANALYZERS do read some of them — `meaning` says which.

### `project_id`

The project this row belongs to. Referenced by uuid and never by a displayable name (`uuid-identity`, G1); cascades on project delete.

| | |
|---|---|
| Type | `uuid`, `NOT NULL` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20250904105527_e00a21a9-0120-48a7-a62d-e2db83fd4cc6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> The engine reads the tier-2 dataset through `datamap.py` and never the deep-tier graph, so no column of this table reaches it. The two centrality ANALYZERS do read some of them — `meaning` says which.

### `plant_name`

The plant label the row was uploaded under. NOT a project scope — a plant name is not unique across projects, which is the ambiguity `analysis_mark_critical_nodes` had to work around by deriving the project from the rows it was given.

| | |
|---|---|
| Type | `text`, `NOT NULL` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20250904105527_e00a21a9-0120-48a7-a62d-e2db83fd4cc6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> The engine reads the tier-2 dataset through `datamap.py` and never the deep-tier graph, so no column of this table reaches it. The two centrality ANALYZERS do read some of them — `meaning` says which.

### `organization`

Legacy organization display string, defaulted to `default_org`. A DISPLAY name and never a join key (`uuid-identity`, G1): the live predicate is `org_is_current_user_org`, uuid-only since WP 3.0 (D29).

| | |
|---|---|
| Type | `text`, `NOT NULL`, default `'default_org'` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20250904105527_e00a21a9-0120-48a7-a62d-e2db83fd4cc6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> The engine reads the tier-2 dataset through `datamap.py` and never the deep-tier graph, so no column of this table reaches it. The two centrality ANALYZERS do read some of them — `meaning` says which.

### `created_by`

The user who created the row, as the uploader asserted it. This application authenticates against `approved_users` rather than Supabase Auth, so the id is CLIENT-ASSERTED — a real constraint, not proof of identity (D28).

| | |
|---|---|
| Type | `uuid` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20250904105527_e00a21a9-0120-48a7-a62d-e2db83fd4cc6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> The engine reads the tier-2 dataset through `datamap.py` and never the deep-tier graph, so no column of this table reaches it. The two centrality ANALYZERS do read some of them — `meaning` says which.

### `uploaded_by`

The user the upload named. Same client-asserted caveat as `created_by` (D28).

| | |
|---|---|
| Type | `uuid` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20250904105527_e00a21a9-0120-48a7-a62d-e2db83fd4cc6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> The engine reads the tier-2 dataset through `datamap.py` and never the deep-tier graph, so no column of this table reaches it. The two centrality ANALYZERS do read some of them — `meaning` says which.

### `created_at`

Row insert time, maintained by the database.

| | |
|---|---|
| Type | `timestamp with time zone`, `NOT NULL`, default `now()` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20250904105527_e00a21a9-0120-48a7-a62d-e2db83fd4cc6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> The engine reads the tier-2 dataset through `datamap.py` and never the deep-tier graph, so no column of this table reaches it. The two centrality ANALYZERS do read some of them — `meaning` says which.

### `updated_at`

Row update time, maintained by the database. When a row was TOUCHED is not what it SAYS, which is why `input-hash` (I5) is anchored on `computed_from_hash` and not on this (§4 D12).

| | |
|---|---|
| Type | `timestamp with time zone`, `NOT NULL`, default `now()` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20250904105527_e00a21a9-0120-48a7-a62d-e2db83fd4cc6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> The engine reads the tier-2 dataset through `datamap.py` and never the deep-tier graph, so no column of this table reaches it. The two centrality ANALYZERS do read some of them — `meaning` says which.

### `src_uid`

Source node `uid`. Part of the natural key with `project_id` and `dst_uid`; READ BY THE PROMINENCE ANALYSIS, so it is digested by `network_topology_hash`.

| | |
|---|---|
| Type | `text`, `NOT NULL` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20250904105527_e00a21a9-0120-48a7-a62d-e2db83fd4cc6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> The engine reads the tier-2 dataset through `datamap.py` and never the deep-tier graph, so no column of this table reaches it. The two centrality ANALYZERS do read some of them — `meaning` says which.

### `dst_uid`

Destination node `uid`. Part of the natural key; digested by `network_topology_hash`.

| | |
|---|---|
| Type | `text`, `NOT NULL` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20250904105527_e00a21a9-0120-48a7-a62d-e2db83fd4cc6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> The engine reads the tier-2 dataset through `datamap.py` and never the deep-tier graph, so no column of this table reaches it. The two centrality ANALYZERS do read some of them — `meaning` says which.

### `relation_type`

What the edge represents (supplier, customer, …), as uploaded.

| | |
|---|---|
| Type | `text` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20250904105527_e00a21a9-0120-48a7-a62d-e2db83fd4cc6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> The engine reads the tier-2 dataset through `datamap.py` and never the deep-tier graph, so no column of this table reaches it. The two centrality ANALYZERS do read some of them — `meaning` says which.

### `relative_revenue`

The share of revenue this relationship carries, as uploaded. READ BY THE PROMINENCE ANALYSIS as the edge weight, so it is digested by `network_topology_hash` — changing it changes every centrality in the project.

| | |
|---|---|
| Type | `numeric` |
| Grain | `level` |
| Unit | dimensionless |
| Added by | `20250904105527_e00a21a9-0120-48a7-a62d-e2db83fd4cc6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> The engine reads the tier-2 dataset through `datamap.py` and never the deep-tier graph, so no column of this table reaches it. The two centrality ANALYZERS do read some of them — `meaning` says which.

### `relative_revenue_percentage`

The same share expressed as a percentage. Uploaded, and read by no analysis — a second spelling of one fact, which is the shape `single-source` (I1) exists to refuse. Named for the WP 6.2 sweep.

| | |
|---|---|
| Type | `numeric` |
| Grain | `level` |
| Unit | dimensionless |
| Added by | `20250904105527_e00a21a9-0120-48a7-a62d-e2db83fd4cc6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> The engine reads the tier-2 dataset through `datamap.py` and never the deep-tier graph, so no column of this table reaches it. The two centrality ANALYZERS do read some of them — `meaning` says which.

### `depth`

Tier distance of the edge from the seed, as uploaded.

| | |
|---|---|
| Type | `integer` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20250904105527_e00a21a9-0120-48a7-a62d-e2db83fd4cc6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> The engine reads the tier-2 dataset through `datamap.py` and never the deep-tier graph, so no column of this table reaches it. The two centrality ANALYZERS do read some of them — `meaning` says which.

### `direction`

Upstream or downstream relative to the seed, as uploaded.

| | |
|---|---|
| Type | `text` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20250904105527_e00a21a9-0120-48a7-a62d-e2db83fd4cc6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> The engine reads the tier-2 dataset through `datamap.py` and never the deep-tier graph, so no column of this table reaches it. The two centrality ANALYZERS do read some of them — `meaning` says which.

## Indexes

| Index | Columns | Unique | Added by |
|---|---|---|---|
| `idx_network_edges_project_id` | `project_id` | no | `20250908075907_36523d34-2b67-4f34-a20b-7076c1698395.sql` |

---

*Generated from data contract `60c9c20a0864`, engine `0.2.3`,
sidecar `supabase/contract/network_edges.contract.yaml`, table created by `20250904105527_e00a21a9-0120-48a7-a62d-e2db83fd4cc6.sql`. No wall-clock date: a generated
page that differs from itself tomorrow cannot be drift-gated.*
