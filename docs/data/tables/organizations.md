<!-- GENERATED by scripts/data-contract/generate.mjs from supabase/contract/organizations.contract.yaml + build/schema.introspected.json + the engine registry. Do not edit by hand: `npm run contract:check` fails on drift. -->

# `organizations`

> **GENERATED** — rendered from the data contract. Edit [`supabase/contract/organizations.contract.yaml`](../../../supabase/contract/organizations.contract.yaml)
> for what the columns mean, or the migrations for what the schema is. Do not edit
> this page: `npm run contract:check` fails when it differs from what the contract
> generates.

**Tier G** — governance — identity, capability, delegation, audit · owned by `platform` · `public.organizations`

**One row is** One tenant. Every project, every dataset and every simulation result in the product belongs to exactly one of these rows, and the boundary between two of them is the boundary the whole access layer is built to hold.

Created by `20260709000002_super_admin_phase1.sql`, which seeded one row per DISTINCT `approved_users.organization` string. That seeding is why the uuid plane and the text plane started out agreeing, and why nothing has kept them agreeing since: the seed ran once, `set_project_defaults()` never stamped the uuid (D27), and `admin_update_organization` renames without propagating. WP 2.1 fixed the trigger, backfilled what it could match unambiguously, and routed every live comparison through one predicate. It deliberately did NOT re-seed this table from distinct strings: after a rename, a user carrying the new name slugifies to a slug no row holds, and seeding would split one organization into two.

## Uniqueness

| Columns | Source | Constraint |
|---|---|---|
| `id` | column PRIMARY KEY | `organizations_pkey` |
| `slug` | column UNIQUE | `organizations_slug_key` |

## Constraints

These reject the row outright. A value that fails one of them does not arrive
partially or get corrected — the write fails.

| Constraint | Rule | Added by |
|---|---|---|
| `organizations_status_check` | `CHECK (status IN ('active','suspended'))` | `20260709000002_super_admin_phase1.sql` |

## Governance

| | |
|---|---|
| Read capability | any project member |
| Write capability | `super_admin` |
| Minimum project role | `viewer` |
| Tier transitions audited | yes |
| Row-level security | enabled |
| Policies on the table | 2 — all carry a predicate |

`write: super_admin` is enforced, not aspirational: every mutation goes through `admin_create_organization` / `admin_update_organization` / `admin_set_org_status`, each of which opens with `_assert_super_admin` and closes with `log_admin_action`. This is one of the few tables where `audited: true` is literally true today — WP 2.3 is what makes the claim general. Read is open to any member of the org via the self-bridge policy below, which WP 2.1 rewrote: it matched the caller's TEXT org against `name` or `slug`, so renaming an organization revoked its own members' read on the row that had just been renamed. It now bridges on `id` first.

<details><summary>2 RLS policies</summary>

| Policy | Command | Roles | Added by |
|---|---|---|---|
| orgs: super admin full | ALL | all | `20260709000002_super_admin_phase1.sql` |
| orgs: members read own | SELECT | all | `20260918000001_org_row_uuid_only.sql` |

</details>

## Where this data is read

| Page | Via | Evidence | Confirmed |
|---|---|---|---|
| `DeveloperApi.tsx` | rpc list_api_keys | `src/pages/DeveloperApi.tsx:261` | yes |

Each row says the page READS the table by that path, at that line. It does
not say every column below is displayed there — a column carries its own
lineage only where an explicit `select` names it. `npm run contract:check`
R12 re-opens every evidence line on each run, so an entry cannot go stale
unnoticed.

## Columns

`CSV header` is the name the **user types**, which is not always the column name —
that gap is defect D21. A dash means the column has no CSV origin.

| Column | CSV header | Type | Unit | Required in CSV | Meaning |
|---|---|---|---|---|---|
| `id` 🔑 | — | `uuid` | — | — | The organization's identity. This is the value that must travel — it is what `projects.organization_id`, `approved_users.organization_id` and the public /v1 API's `p_org_id` all mean. Not displayable, and that is the property that makes it safe as a join key (§2.1 `uuid-identity`). |
| `name` | — | `text` | — | — | The organization's display name, as an administrator typed it. EDITABLE and NOT UNIQUE — two organizations may legitimately share one. It is a label for humans and it is never an identity. |
| `slug` | — | `text` | — | — | A URL-safe form of the name, `lower(regexp_replace(name, '[^a-zA-Z0-9]+', '-', 'g'))` at the moment the row was seeded. UNIQUE, and deliberately NOT recomputed on rename — so after a rename it is a record of what the organization used to be called, which is what makes it usable to match rows written before it. |
| `owner_user_id` | — | `uuid` | — | — | The approved user who owns the tenant. Advisory today — nothing in RLS or any RPC reads it; ownership is expressed through `organization_members.org_role = 'owner'`. Two places holding the same fact is an I1 violation waiting to diverge; WP 2.2 owns the resolution. |
| `status` | — | `text` | — | — | Whether the tenant is live — `active` or `suspended`, CHECK-constrained to those two. Set through `admin_set_org_status`. |
| `settings` | — | `jsonb` | — | — | Per-tenant configuration, free-form. No key of it is read by any migration, edge function or page today; it is a forward-declared extension point. |
| `created_at` | — | `timestamp with time zone` | — | — | When the organization was created. Server-stamped. |
| `updated_at` | — | `timestamp with time zone` | — | — | When the row was last modified. Server-stamped by DEFAULT only — no trigger maintains it, and `admin_update_organization` does not set it, so a renamed organization still shows its creation time here. |

## Each column in full

### `id`

The organization's identity. This is the value that must travel — it is what `projects.organization_id`, `approved_users.organization_id` and the public /v1 API's `p_org_id` all mean. Not displayable, and that is the property that makes it safe as a join key (§2.1 `uuid-identity`).

| | |
|---|---|
| Type | `uuid`, `NOT NULL`, default `gen_random_uuid()` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20260709000002_super_admin_phase1.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `name`

The organization's display name, as an administrator typed it. EDITABLE and NOT UNIQUE — two organizations may legitimately share one. It is a label for humans and it is never an identity.

| | |
|---|---|
| Type | `text`, `NOT NULL` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260709000002_super_admin_phase1.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | non-empty after btrim — admin_update_organization |
| Rendered at | *not yet recorded (WP 5.1)* |

> D13's join key. `admin_update_organization` updates this column and nothing else: no trigger, no cascade, and no second UPDATE reaching `approved_users.organization` or `projects.organization`. So a rename leaves those two text copies holding the OLD string, and the next user approved into the organization carries the NEW one — at which point a text comparison between them can never match again. WP 2.1 routes every such comparison through `org_is_current_user_org(uuid, text)`.

### `slug`

A URL-safe form of the name, `lower(regexp_replace(name, '[^a-zA-Z0-9]+', '-', 'g'))` at the moment the row was seeded. UNIQUE, and deliberately NOT recomputed on rename — so after a rename it is a record of what the organization used to be called, which is what makes it usable to match rows written before it.

| | |
|---|---|
| Type | `text`, `NOT NULL` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20260709000002_super_admin_phase1.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | unique |
| Rendered at | *not yet recorded (WP 5.1)* |

### `owner_user_id`

The approved user who owns the tenant. Advisory today — nothing in RLS or any RPC reads it; ownership is expressed through `organization_members.org_role = 'owner'`. Two places holding the same fact is an I1 violation waiting to diverge; WP 2.2 owns the resolution.

| | |
|---|---|
| Type | `uuid` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20260709000002_super_admin_phase1.sql` |
| References | `public.approved_users(id)` ON DELETE SET NULL |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `status`

Whether the tenant is live — `active` or `suspended`, CHECK-constrained to those two. Set through `admin_set_org_status`.

| | |
|---|---|
| Type | `text`, `NOT NULL`, default `'active'` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260709000002_super_admin_phase1.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | one of active / suspended |
| Rendered at | *not yet recorded (WP 5.1)* |

> SUSPENSION IS NOT ENFORCED IN THE DATA LAYER. No policy and no RPC consults this column before granting access — a suspended organization's members read and write exactly as before. It is a label the admin pages render. Recorded here rather than left to be discovered, per §5 T1; the enforcement belongs with WP 2.2's resolver, which is where a capability decision is made once instead of inevery policy.

### `settings`

Per-tenant configuration, free-form. No key of it is read by any migration, edge function or page today; it is a forward-declared extension point.

| | |
|---|---|
| Type | `jsonb`, `NOT NULL`, default `'{}'::jsonb` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260709000002_super_admin_phase1.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `created_at`

When the organization was created. Server-stamped.

| | |
|---|---|
| Type | `timestamp with time zone`, `NOT NULL`, default `now()` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260709000002_super_admin_phase1.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `updated_at`

When the row was last modified. Server-stamped by DEFAULT only — no trigger maintains it, and `admin_update_organization` does not set it, so a renamed organization still shows its creation time here.

| | |
|---|---|
| Type | `timestamp with time zone`, `NOT NULL`, default `now()` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260709000002_super_admin_phase1.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

---

*Generated from data contract `81c7c8c0e8bb`, engine `0.2.3`,
sidecar `supabase/contract/organizations.contract.yaml`, table created by `20260709000002_super_admin_phase1.sql`. No wall-clock date: a generated
page that differs from itself tomorrow cannot be drift-gated.*
