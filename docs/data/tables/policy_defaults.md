<!-- GENERATED by scripts/data-contract/generate.mjs from supabase/contract/policy_defaults.contract.yaml + build/schema.introspected.json + the engine registry. Do not edit by hand: `npm run contract:check` fails on drift. -->

# `policy_defaults`

> **GENERATED** — rendered from the data contract. Edit [`supabase/contract/policy_defaults.contract.yaml`](../../../supabase/contract/policy_defaults.contract.yaml)
> for what the columns mean, or the migrations for what the schema is. Do not edit
> this page: `npm run contract:check` fails when it differs from what the contract
> generates.

**Tier 4** — decisions — policies, overrides, scenarios · owned by `policy-ui` · `public.policy_defaults`

**One row is** One row per project: the policy each family runs with unless a specific node overrides it. The bundle, in other words — and the thing a policy_override is a patch against.

## Uniqueness

| Columns | Source | Constraint |
|---|---|---|
| `project_id` | column PRIMARY KEY | `policy_defaults_pkey` |

## Constraints

These reject the row outright. A value that fails one of them does not arrive
partially or get corrected — the write fails.

| Constraint | Rule | Added by |
|---|---|---|
| `policy_defaults_strategy_chk` | `CHECK (fulfillment_strategy IN ('make_to_stock','make_to_order','assemble_to_order','engineer_to_order','configure_to_order'))` | `20260607133944_444b6c58-6239-4b6d-89e3-deb6d075de97.sql` |

| Constraint | Kind | Definition |
|---|---|---|
| `policy_defaults_project_fk` | FOREIGN KEY | `FOREIGN KEY (project_id) REFERENCES public.projects(id) ON DELETE CASCADE` |

## Governance

| | |
|---|---|
| Read capability | any project member |
| Write capability | `data_editing` |
| Minimum project role | `editor` |
| Tier transitions audited | yes |
| Row-level security | enabled |
| Policies on the table | 5 — **2 with no predicate** |

`write` names the capability that exists today. WP 2.2 splits `data_editing` into `data_edit_inputs` (tier 2) and `data_edit_policies` (tier 4); this row becomes `data_edit_policies` then.

> **What the database actually permits is wider than the row above.**
> 2 policies here grant access with
> **no predicate at all** (`USING (true)`), so the capability named above is what the
> product intends to check, not what the database enforces:
>
> - `Authenticated users can read policy defaults` — `SELECT` to `authenticated`
> - `Anon can read policy defaults` — `SELECT` to `anon`, `authenticated`
>
> See PLAN.md D28: the application runs as the
> `anon` role with no auth session and the anon key ships in the frontend bundle, so
> closing these is a migration with an auth model behind it rather than a policy edit.

<details><summary>5 RLS policies</summary>

| Policy | Command | Roles | Added by |
|---|---|---|---|
| Authenticated users can read policy defaults | SELECT | authenticated | `20260607055908_b3e74750-d55a-4eb6-8bdc-460bc4cb90a6.sql` |
| Authenticated users can upsert policy defaults | INSERT | authenticated | `20260607055908_b3e74750-d55a-4eb6-8bdc-460bc4cb90a6.sql` |
| Authenticated users can update policy defaults | UPDATE | authenticated | `20260607055908_b3e74750-d55a-4eb6-8bdc-460bc4cb90a6.sql` |
| Admins can delete policy defaults | DELETE | authenticated | `20260607055908_b3e74750-d55a-4eb6-8bdc-460bc4cb90a6.sql` |
| Anon can read policy defaults | SELECT | anon, authenticated | `20260919000010_anon_policies_widen.sql` |

</details>

## Where this data is read

| Page | Via | Evidence | Confirmed |
|---|---|---|---|
| `ProjectIntelligence.tsx` | table read | `src/hooks/usePolicies.tsx:141` | yes |
| `ProjectPolicies.tsx` | table read | `src/hooks/usePolicies.tsx:141` | yes |
| `SimulationLab.tsx` | table read | `src/hooks/usePolicies.tsx:141` | yes |

Each row says the page READS the table by that path, at that line. It does
not say every column below is displayed there — a column carries its own
lineage only where an explicit `select` names it. `npm run contract:check`
R12 re-opens every evidence line on each run, so an entry cannot go stale
unnoticed.

## Columns

`CSV header` is the name the **user types**, which is not always the column name —
that gap is defect D21. A dash means the column has no CSV origin.

| Column | CSV header | Type | Unit | Required in CSV | Meaning |
|---|---|---|---|---|---|
| `project_id` 🔑 | — | `uuid` | — | — | The project these defaults belong to. It is the PRIMARY KEY — one bundle per project, enforced. |
| `sourcing` | — | `jsonb` | — | — | The sourcing family's parameters — how supply is split across suppliers, and what happens when one fails. |
| `inventory` | — | `jsonb` | — | — | The inventory family's parameters — reorder points, order-up-to levels, safety stock and holding cost. |
| `transport` | — | `jsonb` | — | — | The transport family's parameters. Stored and versioned; consumed by no engine mapping today (blueprint G1/G7). |
| `fulfillment` | — | `jsonb` | — | — | The fulfillment family's parameters — how demand is allocated when it cannot all be met. |
| `updated_by` | — | `uuid` | — | — | Who last changed the bundle. |
| `updated_at` | — | `timestamp with time zone` | — | — | When the bundle last changed. Server-set. |
| `created_at` | — | `timestamp with time zone` | — | — | When the bundle was created. Server-set. |
| `production` | — | `jsonb` | — | — | The production family's parameters — capacity and lot sizing at the plant. |
| `recovery` | — | `jsonb` | — | — | The recovery family's parameters — the response list a disruption triggers. |
| `preset_id` | — | `uuid` | — | — | The policy preset this bundle was seeded from, if any. |
| `demand` | — | `jsonb` | — | — | The demand family's parameters. Stored and versioned; the engine takes demand from product data and outbound volume instead (blueprint §2.4). |
| `fulfillment_strategy` | — | `text` | — | — | The project-wide make-to-order / make-to-stock choice, used when a product does not state its own `fulfillment_mode`. |
| `active_preset` | — | `text` | — | — | The name of the preset currently applied, for display beside the grid. |
| `preset_applied_at` | — | `timestamp with time zone` | — | — | When the preset was last applied. |

## Each column in full

### `project_id`

The project these defaults belong to. It is the PRIMARY KEY — one bundle per project, enforced.

| | |
|---|---|
| Type | `uuid`, `NOT NULL` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20260607055908_b3e74750-d55a-4eb6-8bdc-460bc4cb90a6.sql` |
| Read by the engine | `project_map.py::_map_policies -> the project's policy bundle` |
| Transform | filter, never read as a value |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `sourcing`

The sourcing family's parameters — how supply is split across suppliers, and what happens when one fails.

| | |
|---|---|
| Type | `jsonb`, `NOT NULL`, default `'{}'::jsonb` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260607055908_b3e74750-d55a-4eb6-8bdc-460bc4cb90a6.sql` |
| Read by the engine | `project_map.py::_map_policies -> P-S.x sourcing plugins` |
| Transform | a JSON object of family parameters, mapped to engine plugins by activation rules |
| When NULL, the engine uses | {} — the engine's own defaults for every plugin in the family |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `inventory`

The inventory family's parameters — reorder points, order-up-to levels, safety stock and holding cost.

| | |
|---|---|
| Type | `jsonb`, `NOT NULL`, default `'{}'::jsonb` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260607055908_b3e74750-d55a-4eb6-8bdc-460bc4cb90a6.sql` |
| Read by the engine | `project_map.py::_map_policies -> P-P.x / inventory_control plugins` |
| Transform | a JSON object of family parameters, mapped to engine plugins by activation rules |
| When NULL, the engine uses | {} — the engine's own defaults for every plugin in the family |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `transport`

The transport family's parameters. Stored and versioned; consumed by no engine mapping today (blueprint G1/G7).

| | |
|---|---|
| Type | `jsonb`, `NOT NULL`, default `'{}'::jsonb` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260607055908_b3e74750-d55a-4eb6-8bdc-460bc4cb90a6.sql` |
| Read by the engine | `project_map.py::_map_policies -> no plugin — stored, not read` |
| Transform | a JSON object of family parameters, mapped to engine plugins by activation rules |
| When NULL, the engine uses | {} — the engine's own defaults for every plugin in the family |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `fulfillment`

The fulfillment family's parameters — how demand is allocated when it cannot all be met.

| | |
|---|---|
| Type | `jsonb`, `NOT NULL`, default `'{}'::jsonb` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260607055908_b3e74750-d55a-4eb6-8bdc-460bc4cb90a6.sql` |
| Read by the engine | `project_map.py::_map_policies -> customer_allocation and related P-C.x plugins` |
| Transform | a JSON object of family parameters, mapped to engine plugins by activation rules |
| When NULL, the engine uses | {} — the engine's own defaults for every plugin in the family |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `updated_by`

Who last changed the bundle.

| | |
|---|---|
| Type | `uuid` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20260607055908_b3e74750-d55a-4eb6-8bdc-460bc4cb90a6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> Governance, not a parameter. The data plane's only actor stamp on tier 4; WP 2.3 turns it into an audit row.

### `updated_at`

When the bundle last changed. Server-set.

| | |
|---|---|
| Type | `timestamp with time zone`, `NOT NULL`, default `now()` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260607055908_b3e74750-d55a-4eb6-8bdc-460bc4cb90a6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `created_at`

When the bundle was created. Server-set.

| | |
|---|---|
| Type | `timestamp with time zone`, `NOT NULL`, default `now()` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260607055908_b3e74750-d55a-4eb6-8bdc-460bc4cb90a6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `production`

The production family's parameters — capacity and lot sizing at the plant.

| | |
|---|---|
| Type | `jsonb`, `NOT NULL`, default `'{}'::jsonb` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260607115833_78440629-51bc-4ad3-9934-f6c2f357ff86.sql` |
| Read by the engine | `project_map.py::_map_policies -> P-P.x production plugins` |
| Transform | a JSON object of family parameters, mapped to engine plugins by activation rules |
| When NULL, the engine uses | {} — the engine's own defaults for every plugin in the family |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `recovery`

The recovery family's parameters — the response list a disruption triggers.

| | |
|---|---|
| Type | `jsonb`, `NOT NULL`, default `'{}'::jsonb` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260607115833_78440629-51bc-4ad3-9934-f6c2f357ff86.sql` |
| Read by the engine | `project_map.py::_map_policies -> recovery_response_to_policy in engineBridge.json` |
| Transform | a JSON object of family parameters, mapped to engine plugins by activation rules |
| When NULL, the engine uses | {} — the engine's own defaults for every plugin in the family |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `preset_id`

The policy preset this bundle was seeded from, if any.

| | |
|---|---|
| Type | `uuid` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20260607115833_78440629-51bc-4ad3-9934-f6c2f357ff86.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> A UI affordance: it records where the bundle came from so the preset can be re-applied. Not read by the engine mapping.

### `demand`

The demand family's parameters. Stored and versioned; the engine takes demand from product data and outbound volume instead (blueprint §2.4).

| | |
|---|---|
| Type | `jsonb`, `NOT NULL`, default `'{}'::jsonb` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260607133944_444b6c58-6239-4b6d-89e3-deb6d075de97.sql` |
| Read by the engine | `project_map.py::_map_policies -> no plugin — stored, not read` |
| Transform | a JSON object of family parameters, mapped to engine plugins by activation rules |
| When NULL, the engine uses | {} — the engine's own defaults for every plugin in the family |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `fulfillment_strategy`

The project-wide make-to-order / make-to-stock choice, used when a product does not state its own `fulfillment_mode`.

| | |
|---|---|
| Type | `text`, `NOT NULL`, default `'make_to_stock'` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260607133944_444b6c58-6239-4b6d-89e3-deb6d075de97.sql` |
| Read by the engine | `project_map.py::_fulfillment_mode -> the per-product fallback` |
| Transform | read when products.fulfillment_mode is NULL or unrecognised |
| When NULL, the engine uses | make_to_stock — the column default |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `active_preset`

The name of the preset currently applied, for display beside the grid.

| | |
|---|---|
| Type | `text` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260607133944_444b6c58-6239-4b6d-89e3-deb6d075de97.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> Display only; `preset_id` is the join key. Not read by the engine.

### `preset_applied_at`

When the preset was last applied.

| | |
|---|---|
| Type | `timestamp with time zone` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260607133944_444b6c58-6239-4b6d-89e3-deb6d075de97.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> Display only. Not read by the engine.

---

*Generated from data contract `45218efefab2`, engine `0.2.3`,
sidecar `supabase/contract/policy_defaults.contract.yaml`, table created by `20260607055908_b3e74750-d55a-4eb6-8bdc-460bc4cb90a6.sql`. No wall-clock date: a generated
page that differs from itself tomorrow cannot be drift-gated.*
