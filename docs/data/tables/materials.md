<!-- GENERATED by scripts/data-contract/generate.mjs from supabase/contract/materials.contract.yaml + build/schema.introspected.json + the engine registry. Do not edit by hand: `npm run contract:check` fails on drift. -->

# `materials`

> **GENERATED** — rendered from the data contract. Edit [`supabase/contract/materials.contract.yaml`](../../../supabase/contract/materials.contract.yaml)
> for what the columns mean, or the migrations for what the schema is. Do not edit
> this page: `npm run contract:check` fails when it differs from what the contract
> generates.

**Tier 2** — canonical — the only tier humans edit · owned by `data-ingestion` · `public.materials`

**One row is** One material in one project: the economics the simulation reads for it. The precision path — the CSV lanes carry prices too, and where this row is silent the engine derives the value from them rather than treating it as missing.

## Uniqueness

| Columns | Source | Constraint |
|---|---|---|
| `project_id` + `material_id` | PRIMARY KEY constraint | — |

## Constraints

| Constraint | Kind | Definition |
|---|---|---|
| — | PRIMARY KEY | `PRIMARY KEY (project_id, material_id)` |
| `materials_source_row_fk` | FOREIGN KEY | `FOREIGN KEY (source_row_id) REFERENCES public.ingest_staged_rows(id) ON DELETE SET NULL DEFERRABLE INITIALLY DEFERRED` |

## Governance

| | |
|---|---|
| Read capability | any project member |
| Write capability | `data_editing` |
| Minimum project role | `editor` |
| Tier transitions audited | yes |
| Row-level security | **cannot be determined from the migrations** |
| Policies on the table | **none** |

`write` names the capability that exists today. WP 2.2 splits `data_editing` into `data_edit_inputs` (tier 2) and `data_edit_policies` (tier 4); this row becomes `data_edit_inputs` then, and all thirteen sidecars change together. SEPARATELY — AND THIS REPLACES WHAT WP 1.2 WROTE HERE, WHICH WAS WRONG: this sidecar used to assert `rls_enabled: false` and say "no migration ever runs ALTER TABLE ... ENABLE ROW LEVEL SECURITY on them". One does. `20260614000001_item_master.sql:57-66` enables RLS and creates two policies (`%s_auth_all`, `%s_anon_read`) on each of the three masters — but through `EXECUTE format(...)` inside a `FOREACH` over an array of table names, which a static replay of the migrations cannot evaluate. The introspector recorded the absence as `enabled: false`, WP 1.2 read that as fact, and the contract was one generated page away from telling a user their item masters were unprotected. WP 1.4 fixed the mechanism, not the guess: the introspector now records dynamic DDL (`dynamic_ddl`) and marks the tables it touches `rls.determinate: false`, the validator refuses to let a sidecar assert an RLS state the migrations do not settle, and the generated page says the state is indeterminate and names the migration. The ONLY way to know is to read the live database — PLAN.md §15, still unrun. WP 2.4's security review owns it.

> **The migrations do not settle whether RLS is on here.**
> `20260614000001_item_master.sql` sets it with
> **dynamic SQL** — `EXECUTE format(...)`, assembled at run time — which a static replay
> of the migration history cannot evaluate. The honest answer is *unknown*, and this
> page will not round it to *off*: a security review that starts from an invented
> "unprotected" is as wrong as one that starts from an invented "protected".
> Only reading the live database answers it (PLAN.md §15).

## Columns

`CSV header` is the name the **user types**, which is not always the column name —
that gap is defect D21. A dash means the column has no CSV origin.

| Column | CSV header | Type | Unit | Required in CSV | Meaning |
|---|---|---|---|---|---|
| `project_id` | — | `uuid` | — | — | The project this material belongs to. Half of the primary key. |
| `material_id` | `material_id` | `text` | — | **yes** | The material's code as the user's own systems spell it. Joins to the BOM and to every inbound arc that supplies it. |
| `name` | `name` | `text` | — | no | A human-readable name for the material. Display only. |
| `cost` | `cost` | `numeric` | `currency per unit of material` | no | What one unit of this material costs to buy. The master value; where it is unset the engine uses the cheapest inbound arc instead, which is usually what the user wants and is why leaving it blank is not an error. |
| `holding_cost_pct` | `holding_cost_pct` | `numeric` | `fraction of unit value per year` | no | The annual cost of holding one unit in stock, as a fraction of its value. 0.2 means holding a unit for a year costs a fifth of what the unit is worth. |
| `moq` | `moq` | `numeric` | `units of material` | no | Minimum order quantity: the smallest amount this material can be ordered in. A commercial fact the supplier states, not something to be inferred from order history. |
| `initial_on_hand` | `initial_on_hand` | `numeric` | `units of material` | no | How much of this material is in stock when the simulation starts. |
| `lead_time_dist` | `lead_time_dist` | `text` | — | no | The SHAPE of this material's delivery lead time — deterministic, or a named distribution around the arc's lead time. `deterministic` means the lead time is exactly what the arc says, every time. |
| `lead_time_cv` | `lead_time_cv` | `numeric` | `coefficient of variation (dimensionless)` | no | How much the lead time varies, as a coefficient of variation. Read only when `lead_time_dist` is not deterministic: the engine skips sampling entirely when the distribution is deterministic or the CV is zero. |
| `updated_at` | — | `timestamp with time zone` | — | — | When the row last changed. Server-set. |
| `created_at` | — | `timestamp with time zone` | — | — | When the row was inserted. Server-set. |
| `source_system` | — | `text` | — | — | Which external system this row came from, when it was synced rather than uploaded. NULL for a hand-uploaded or hand-edited row. |
| `source_external_id` | — | `text` | — | — | This row's identifier in the external system, so a re-sync can match it. |
| `source_synced_at` | — | `timestamp with time zone` | — | — | When the external system last confirmed this row. |
| `ingest_run_id` | — | `uuid` | — | — | The ingestion run that last wrote this row (WP 3.3, D55), and through it the project, the source kind and who approved the promotion. NULL for every row that predates the CSV landing path — which, on the day this column lands, is every row — and for rows whose run has since been deleted. |
| `source_row_id` | — | `uuid` | — | — | The tier-1 staged row this was promoted from (WP 3.3, D55). Its `source_row_number` is the physical line of the uploaded file, header = line 1. `ON DELETE SET NULL` and DEFERRABLE, because staging dies with its run and an item master belongs to the project rather than to the run. |

## Each column in full

### `project_id`

The project this material belongs to. Half of the primary key.

| | |
|---|---|
| Type | `uuid`, `NOT NULL` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20260614000001_item_master.sql` |
| References | `public.projects(id)` ON DELETE CASCADE |
| Read by the engine | `datamap.py::rows -> per-project fetch` |
| Transform | filter, never read as a value |
| Validated at ingest | set from the selected project, never from the CSV |
| Rendered at | *not yet recorded (WP 5.1)* |

### `material_id`

The material's code as the user's own systems spell it. Joins to the BOM and to every inbound arc that supplies it.

| | |
|---|---|
| Type | `text`, `NOT NULL` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20260614000001_item_master.sql` |
| Read by the engine | `project_map.py::_map_materials -> MaterialRow.id` |
| Transform | str() |
| Validated at ingest | non-empty; unique within the project |
| Rendered at | *not yet recorded (WP 5.1)* |

### `name`

A human-readable name for the material. Display only.

| | |
|---|---|
| Type | `text` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260614000001_item_master.sql` |
| Read by the engine | `project_map.py::_map_materials -> Material.name` |
| Transform | str(name or material_id) |
| When NULL, the engine uses | the material_id itself |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

**Substitutions** — every point where a value you did not supply can stand in
for one you did.

| When | The value used | Shown as | Visible where |
|---|---|---|---|
| NULL or blank | the material_id | `default` | — |

**Resolution** — how a value is decided when more than one source could supply one.

| | |
|---|---|
| Default mode | `asserted` |
| Assertable by | `csv_upload`, `item_master_form` |
| Estimable from | **never** — this value may not be estimated |
| Hybrid (centre / spread) | **none** — the engine has no variability field for this quantity |
| On conflict | `assertion_wins` |

A label. Recorded for completeness: every engine-consumed field carries a resolution block, including the ones where the answer is trivial.

### `cost`

What one unit of this material costs to buy. The master value; where it is unset the engine uses the cheapest inbound arc instead, which is usually what the user wants and is why leaving it blank is not an error.

| | |
|---|---|
| Type | `numeric` |
| Grain | `level` |
| Unit | `currency per unit of material` — fixed |
| Added by | `20260614000001_item_master.sql` |
| Read by the engine | `project_map.py::_map_materials -> Material.cost` |
| Transform | float() when > 0 |
| When NULL, the engine uses | the cheapest inbound unit_price for this material, floored at 1.0 |
| Validated at ingest | numeric > 0 |
| Rendered at | *not yet recorded (WP 5.1)* |

**The engine calls this `required`.** Inventory valuation and holding cost — the terminal default of 1.0 makes every cost KPI meaningless.

Fallback chain, from the engine's own registry: cheapest_inbound_price (info) → constant 1 (warn).

**Substitutions** — every point where a value you did not supply can stand in
for one you did.

| When | The value used | Shown as | Visible where |
|---|---|---|---|
| NULL or <= 0 | the cheapest inbound arc's unit_price (grading.ts::cheapestInboundCost floors a <= 0 price to 1.0 first) | `derived` | the effective-economics badge in /policies |

**Resolution** — how a value is decided when more than one source could supply one.

| | |
|---|---|
| Default mode | `asserted` |
| Assertable by | `csv_upload`, `item_master_form` |
| Estimable from | **never** — this value may not be estimated |
| Hybrid (centre / spread) | **none** — the engine has no variability field for this quantity |
| On conflict | `assertion_wins` |

hybrid is NULL because scsim has nowhere to put a spread: cost and unit_price are bare floats on Material and Product, with no distribution, no CV and no sampling path anywhere in the engine. Recording `hybrid: null` here is a statement about the ENGINE, not about the world — prices obviously vary. Giving them a spread is an engine change on the scsim track; see the RFC in PLAN.md §14. Do not invent one in this contract: a contract that describes a capability the engine does not have is how a fallback ends up in code with no contract entry, which is what I6 forbids in the other direction. estimable_from is empty for a second reason: the value already has a derivation — the cheapest inbound arc — and that is a substitution the user can see and trace, not an estimate.

### `holding_cost_pct`

The annual cost of holding one unit in stock, as a fraction of its value. 0.2 means holding a unit for a year costs a fifth of what the unit is worth.

| | |
|---|---|
| Type | `numeric` |
| Grain | `level` |
| Unit | `fraction of unit value per year` — fixed |
| Added by | `20260614000001_item_master.sql` |
| Read by the engine | `project_map.py::_map_materials -> Material.holding_cost_rate` |
| Transform | falls back to the inventory policy bundle's holding_cost_pct when NULL |
| When NULL, the engine uses | the project's inventory policy bundle value (0.2 in the shipped default) |
| Validated at ingest | numeric in [0, 1] |
| Rendered at | *not yet recorded (WP 5.1)* |

**Substitutions** — every point where a value you did not supply can stand in
for one you did.

| When | The value used | Shown as | Visible where |
|---|---|---|---|
| NULL | the inventory bundle's holding_cost_pct | `default` | the policy grid's provenance dot |

**Resolution** — how a value is decided when more than one source could supply one.

| | |
|---|---|
| Default mode | `asserted` |
| Assertable by | `csv_upload`, `item_master_form`, `policy_grid` |
| Estimable from | **never** — this value may not be estimated |
| Hybrid (centre / spread) | **none** — the engine has no variability field for this quantity |
| On conflict | `assertion_wins` |

A financial policy parameter, not an observation: it is what the business decides holding stock costs it. Falls back to the project's inventory bundle, which is a policy default and is visible as one.

### `moq`

Minimum order quantity: the smallest amount this material can be ordered in. A commercial fact the supplier states, not something to be inferred from order history.

| | |
|---|---|
| Type | `numeric` |
| Grain | `level` |
| Unit | `units of material` — fixed |
| Added by | `20260614000001_item_master.sql` |
| Read by the engine | `project_map.py::_map_supply -> SupplierLink.moq` |
| Transform | float(moq) when truthy, else 0.0 — applied per (supplier, material) link |
| When NULL, the engine uses | 0.0 (no minimum) |
| Validated at ingest | numeric >= 0 |
| Rendered at | *not yet recorded (WP 5.1)* |

**Substitutions** — every point where a value you did not supply can stand in
for one you did.

| When | The value used | Shown as | Visible where |
|---|---|---|---|
| NULL or zero | 0.0 — no minimum applies | `default` | — |

**Resolution** — how a value is decided when more than one source could supply one.

| | |
|---|---|
| Default mode | `asserted` |
| Assertable by | `csv_upload`, `item_master_form` |
| Estimable from | **never** — this value may not be estimated |
| Hybrid (centre / spread) | **none** — the engine has no variability field for this quantity |
| On conflict | `assertion_wins` |

estimable_from is EMPTY and that is a positive statement, not an omission. This is an engineering or commercial fact that someone knows exactly — it is written on a drawing, in a contract, or on a machine's nameplate. Fitting a distribution to it would produce a number with error bars around something that has no error, and the error bars would be believed. A future estimator reading this contract must refuse this field. A minimum order quantity is a term the supplier states. Inferring it from observed order sizes learns the ordering policy, not the constraint — and then the simulation optimises against its own past behaviour.

### `initial_on_hand`

How much of this material is in stock when the simulation starts.

| | |
|---|---|
| Type | `numeric` |
| Grain | `level` |
| Unit | `units of material` — fixed |
| Added by | `20260614000001_item_master.sql` |
| Read by the engine | `project_map.py::_map_materials -> Material.initial_on_hand` |
| Transform | float() when not None; None is passed through as None, not as zero |
| When NULL, the engine uses | the engine's own opening-stock rule |
| Validated at ingest | numeric >= 0 |
| Rendered at | *not yet recorded (WP 5.1)* |

**Resolution** — how a value is decided when more than one source could supply one.

| | |
|---|---|
| Default mode | `asserted` |
| Assertable by | `csv_upload`, `item_master_form` |
| Estimable from | `observations.stock_counts (Phase 7+)` |
| Hybrid (centre / spread) | **none** — the engine has no variability field for this quantity |
| On conflict | `assertion_wins` |

Opening stock is a measurement — someone counted it — so it is the one master economic field where an observations tier could legitimately supply it. Nothing does today.

### `lead_time_dist`

The SHAPE of this material's delivery lead time — deterministic, or a named distribution around the arc's lead time. `deterministic` means the lead time is exactly what the arc says, every time.

| | |
|---|---|
| Type | `text` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260614000001_item_master.sql` |
| Read by the engine | `project_map.py::_map_supply -> SupplierLink.lead_time_dist` |
| Transform | LeadTimeDist(value) when set, else LeadTimeDist.DETERMINISTIC |
| When NULL, the engine uses | deterministic |
| Validated at ingest | one of the LeadTimeDist enum values; `empirical` is reserved for the data-import path (M7) and is not selectable yet |
| Rendered at | *not yet recorded (WP 5.1)* |

**Substitutions** — every point where a value you did not supply can stand in
for one you did.

| When | The value used | Shown as | Visible where |
|---|---|---|---|
| NULL or blank | deterministic | `default` | — |

**Resolution** — how a value is decided when more than one source could supply one.

| | |
|---|---|
| Default mode | `asserted` |
| Assertable by | `csv_upload`, `item_master_form` |
| Estimable from | `observations.receipts (Phase 7+)` |
| Hybrid (centre / spread) | **none** — the engine has no variability field for this quantity |
| On conflict | `assertion_wins` |

The SPREAD half of lead_time's hybrid; the centre is inbound_logistics.lead_time. LeadTimeDist.EMPIRICAL exists in the enum and is RESERVED for the data-import path (M7) — it is the value an estimator would write, which is why it is not offered in the UI today. `deterministic` is the default and means the lead time is exactly what the arc says.

### `lead_time_cv`

How much the lead time varies, as a coefficient of variation. Read only when `lead_time_dist` is not deterministic: the engine skips sampling entirely when the distribution is deterministic or the CV is zero.

| | |
|---|---|
| Type | `numeric` |
| Grain | `level` |
| Unit | `coefficient of variation (dimensionless)` — fixed |
| Added by | `20260614000001_item_master.sql` |
| Read by the engine | `project_map.py::_map_supply -> SupplierLink.lead_time_cv` |
| Transform | float() when truthy, else 0.0 |
| When NULL, the engine uses | 0.0 — no variability, so no sampling |
| Validated at ingest | numeric >= 0 |
| Rendered at | *not yet recorded (WP 5.1)* |

**Substitutions** — every point where a value you did not supply can stand in
for one you did.

| When | The value used | Shown as | Visible where |
|---|---|---|---|
| NULL or zero | 0.0 | `default` | — |

**Resolution** — how a value is decided when more than one source could supply one.

| | |
|---|---|
| Default mode | `asserted` |
| Assertable by | `csv_upload`, `item_master_form` |
| Estimable from | `observations.receipts (Phase 7+)` |
| Hybrid (centre / spread) | **none** — the engine has no variability field for this quantity |
| On conflict | `assertion_wins` |

The magnitude of lead_time's spread. Read only when lead_time_dist is not deterministic: core/engine.py tests both, so a CV on a deterministic link is silently inert. Zero and NULL are the same thing here.

### `updated_at`

When the row last changed. Server-set.

| | |
|---|---|
| Type | `timestamp with time zone`, `NOT NULL`, default `now()` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260614000001_item_master.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `created_at`

When the row was inserted. Server-set.

| | |
|---|---|
| Type | `timestamp with time zone`, `NOT NULL`, default `now()` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260614000001_item_master.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `source_system`

Which external system this row came from, when it was synced rather than uploaded. NULL for a hand-uploaded or hand-edited row.

| | |
|---|---|
| Type | `text` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260829120000_erp_connector_phase1_2.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> NOT TRACED into the engine, and correctly so: it is provenance, not a parameter. Read by the ERP connector (blueprint G18), which is the only writer. Kept, not a deletion candidate.

### `source_external_id`

This row's identifier in the external system, so a re-sync can match it.

| | |
|---|---|
| Type | `text` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260829120000_erp_connector_phase1_2.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> NOT TRACED into the engine; ERP-connector provenance, as source_system.

### `source_synced_at`

When the external system last confirmed this row.

| | |
|---|---|
| Type | `timestamp with time zone` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260829120000_erp_connector_phase1_2.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> NOT TRACED into the engine; ERP-connector provenance, as source_system.

### `ingest_run_id`

The ingestion run that last wrote this row (WP 3.3, D55), and through it the project, the source kind and who approved the promotion. NULL for every row that predates the CSV landing path — which, on the day this column lands, is every row — and for rows whose run has since been deleted.

| | |
|---|---|
| Type | `uuid` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20260916000020_item_masters_land.sql` |
| References | `public.ingest_runs(id)` ON DELETE SET NULL |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `source_row_id`

The tier-1 staged row this was promoted from (WP 3.3, D55). Its `source_row_number` is the physical line of the uploaded file, header = line 1. `ON DELETE SET NULL` and DEFERRABLE, because staging dies with its run and an item master belongs to the project rather than to the run.

| | |
|---|---|
| Type | `uuid` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20260916000020_item_masters_land.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

---

*Generated from data contract `80ec518bc194`, engine `0.2.3`,
sidecar `supabase/contract/materials.contract.yaml`, table created by `20260614000001_item_master.sql`. No wall-clock date: a generated
page that differs from itself tomorrow cannot be drift-gated.*
