<!-- GENERATED by scripts/data-contract/generate.mjs from supabase/contract/recovery_playbooks.contract.yaml + build/schema.introspected.json + the engine registry. Do not edit by hand: `npm run contract:check` fails on drift. -->

# `recovery_playbooks`

> **GENERATED** — rendered from the data contract. Edit [`supabase/contract/recovery_playbooks.contract.yaml`](../../../supabase/contract/recovery_playbooks.contract.yaml)
> for what the columns mean, or the migrations for what the schema is. Do not edit
> this page: `npm run contract:check` fails when it differs from what the contract
> generates.

**Tier 4** — decisions — policies, overrides, scenarios · owned by `engine` · `public.recovery_playbooks`

**One row is** One named recovery strategy a scenario can apply after a disruption — the levers, their order and their parameters. A system playbook (`is_system`, `project_id` NULL) ships with the product; a project playbook belongs to one project.

## Uniqueness

| Columns | Source | Constraint |
|---|---|---|
| `id` | column PRIMARY KEY | `recovery_playbooks_pkey` |

## Governance

| | |
|---|---|
| Read capability | any project member |
| Write capability | `data_edit_policies` |
| Minimum project role | `editor` |
| Tier transitions audited | yes |
| Row-level security | enabled |
| Policies on the table | 4 — **1 with no predicate** |

Tier 4 — the DECISION plane: what a person or an agent CHOSE, as against the dataset they chose it over and the result it produced. `result-binding` (I8) needs all three, which is why WP 6.3 keeps the results and this package takes the decisions. Audited from WP 6.4: the three statement-grain triggers land beside this sidecar, because `dataPlaneAudit.test.ts` scopes its rule to tables IN the contract and a deferred table's writes are unaudited with nothing to notice (D54). **Two of the six recovery strategies a scenario can name reach no engine plugin (§4 D114)**, so a playbook's `config` can record an intent the run will not act on. The row is stored faithfully either way; what is missing is a reader that says so, and the manual states it meanwhile.

> **What the database actually permits is wider than the row above.**
> 1 policy here grants access with
> **no predicate at all** (`USING (true)`), so the capability named above is what the
> product intends to check, not what the database enforces:
>
> - `playbooks_select_all_auth` — `SELECT` to `authenticated`
>
> See PLAN.md D28: the application runs as the
> `anon` role with no auth session and the anon key ships in the frontend bundle, so
> closing these is a migration with an auth model behind it rather than a policy edit.

<details><summary>4 RLS policies</summary>

| Policy | Command | Roles | Added by |
|---|---|---|---|
| playbooks_select_all_auth | SELECT | authenticated | `20260607125247_efb67f7c-335b-482c-8292-59a47554251d.sql` |
| playbooks_insert_own | INSERT | authenticated | `20260607125247_efb67f7c-335b-482c-8292-59a47554251d.sql` |
| playbooks_update_own | UPDATE | authenticated | `20260607125247_efb67f7c-335b-482c-8292-59a47554251d.sql` |
| playbooks_delete_own | DELETE | authenticated | `20260607125247_efb67f7c-335b-482c-8292-59a47554251d.sql` |

</details>

## Where this data is read

| Page | Via | Evidence | Confirmed |
|---|---|---|---|
| `SimulationLab.tsx` | table read | `src/hooks/useRecoveryPlaybooks.tsx:26` | yes |

Each row says the page READS the table by that path, at that line. It does
not say every column below is displayed there — a column carries its own
lineage only where an explicit `select` names it. `npm run contract:check`
R12 re-opens every evidence line on each run, so an entry cannot go stale
unnoticed.

## Columns

`CSV header` is the name the **user types**, which is not always the column name —
that gap is defect D21. A dash means the column has no CSV origin.

| Column | CSV header | Type | Unit | Required in CSV | Meaning |
|---|---|---|---|---|---|
| `id` 🔑 | — | `uuid` | — | — | Surrogate key. There is no natural key: two playbooks may share a name, in one project or across projects, and the product treats that as legitimate rather than as a collision. |
| `project_id` | — | `uuid` | — | — | The project this playbook belongs to, or NULL for a system playbook. Nullable on purpose — that NULL is what makes a row part of the shipped catalog, and it is read as such by `useRecoveryPlaybooks`'s `is_system.eq.true,project_id.eq.<id>` filter. |
| `name` | — | `text` | — | — | What a person reads in the picker. Not a key; see `id`. |
| `description` | — | `text` | — | — | One sentence on what the playbook does. Defaults to the empty string rather than NULL, so a reader gets a blank rather than the word `null` — and a blank here means nobody wrote one, not that the playbook does nothing. |
| `is_system` | — | `boolean` | — | — | TRUE for a playbook that ships with the product. The only thing separating the catalog from a user's own work, and the first sort key in the picker. |
| `config` | — | `jsonb` | — | — | THE DECISION ITSELF — which levers, in what order, with what parameters. Free-form JSON: the levers are validated where they are USED rather than where they are stored, which is why D114's two unmapped strategies can be saved at all. |
| `created_by` | — | `uuid` | — | — | The user who created it, where one is recorded. NULL on system rows and on anything written before the column existed — unknown, not nobody. |
| `created_at` | — | `timestamp with time zone` | — | — | When the row was created. Server-stamped, never the client's clock. |
| `updated_at` | — | `timestamp with time zone` | — | — | When it was last changed, server-stamped. |

## Each column in full

### `id`

Surrogate key. There is no natural key: two playbooks may share a name, in one project or across projects, and the product treats that as legitimate rather than as a collision.

| | |
|---|---|
| Type | `uuid`, `NOT NULL`, default `gen_random_uuid()` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20260607125247_efb67f7c-335b-482c-8292-59a47554251d.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `project_id`

The project this playbook belongs to, or NULL for a system playbook. Nullable on purpose — that NULL is what makes a row part of the shipped catalog, and it is read as such by `useRecoveryPlaybooks`'s `is_system.eq.true,project_id.eq.<id>` filter.

| | |
|---|---|
| Type | `uuid` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20260607125247_efb67f7c-335b-482c-8292-59a47554251d.sql` |
| References | `public.projects(id)` ON DELETE CASCADE |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `name`

What a person reads in the picker. Not a key; see `id`.

| | |
|---|---|
| Type | `text`, `NOT NULL` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260607125247_efb67f7c-335b-482c-8292-59a47554251d.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `description`

One sentence on what the playbook does. Defaults to the empty string rather than NULL, so a reader gets a blank rather than the word `null` — and a blank here means nobody wrote one, not that the playbook does nothing.

| | |
|---|---|
| Type | `text`, `NOT NULL`, default `''` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260607125247_efb67f7c-335b-482c-8292-59a47554251d.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `is_system`

TRUE for a playbook that ships with the product. The only thing separating the catalog from a user's own work, and the first sort key in the picker.

| | |
|---|---|
| Type | `boolean`, `NOT NULL`, default `false` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260607125247_efb67f7c-335b-482c-8292-59a47554251d.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `config`

THE DECISION ITSELF — which levers, in what order, with what parameters. Free-form JSON: the levers are validated where they are USED rather than where they are stored, which is why D114's two unmapped strategies can be saved at all.

| | |
|---|---|
| Type | `jsonb`, `NOT NULL`, default `'{}'::jsonb` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260607125247_efb67f7c-335b-482c-8292-59a47554251d.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `created_by`

The user who created it, where one is recorded. NULL on system rows and on anything written before the column existed — unknown, not nobody.

| | |
|---|---|
| Type | `uuid` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20260607125247_efb67f7c-335b-482c-8292-59a47554251d.sql` |
| References | `auth.users(id)` ON DELETE SET NULL |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `created_at`

When the row was created. Server-stamped, never the client's clock.

| | |
|---|---|
| Type | `timestamp with time zone`, `NOT NULL`, default `now()` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260607125247_efb67f7c-335b-482c-8292-59a47554251d.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `updated_at`

When it was last changed, server-stamped.

| | |
|---|---|
| Type | `timestamp with time zone`, `NOT NULL`, default `now()` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260607125247_efb67f7c-335b-482c-8292-59a47554251d.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

## Indexes

| Index | Columns | Unique | Added by |
|---|---|---|---|
| `recovery_playbooks_project_idx` | `project_id` | no | `20260607125247_efb67f7c-335b-482c-8292-59a47554251d.sql` |
| `recovery_playbooks_system_idx` | `is_system` | no | `20260607125247_efb67f7c-335b-482c-8292-59a47554251d.sql` |

---

*Generated from data contract `dc1618b7df79`, engine `0.2.3`,
sidecar `supabase/contract/recovery_playbooks.contract.yaml`, table created by `20260607125247_efb67f7c-335b-482c-8292-59a47554251d.sql`. No wall-clock date: a generated
page that differs from itself tomorrow cannot be drift-gated.*
