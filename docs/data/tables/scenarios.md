<!-- GENERATED by scripts/data-contract/generate.mjs from supabase/contract/scenarios.contract.yaml + build/schema.introspected.json + the engine registry. Do not edit by hand: `npm run contract:check` fails on drift. -->

# `scenarios`

> **GENERATED** — rendered from the data contract. Edit [`supabase/contract/scenarios.contract.yaml`](../../../supabase/contract/scenarios.contract.yaml)
> for what the columns mean, or the migrations for what the schema is. Do not edit
> this page: `npm run contract:check` fails when it differs from what the contract
> generates.

**Tier 4** — decisions — policies, overrides, scenarios · owned by `engine` · `public.scenarios`

**One row is** One what-if a person set up and can run: the horizon, the warm-up, the replications, the seed, the demand model, the disruption schedule and the recovery overrides. The scenario half of `result-binding` (I8) — a run cites one of these rows, and the export reads it back whole.

## Uniqueness

| Columns | Source | Constraint |
|---|---|---|
| `id` | column PRIMARY KEY | `scenarios_pkey` |

## Governance

| | |
|---|---|
| Read capability | any project member |
| Write capability | `data_edit_policies` |
| Minimum project role | `editor` |
| Tier transitions audited | yes |
| Row-level security | enabled |
| Policies on the table | 2 — **2 with no predicate** |

Tier 4 — the DECISION plane: what a person or an agent CHOSE, as against the dataset they chose it over and the result it produced. `result-binding` (I8) needs all three, which is why WP 6.3 keeps the results and this package takes the decisions. Audited from WP 6.4: the three statement-grain triggers land beside this sidecar, because `dataPlaneAudit.test.ts` scopes its rule to tables IN the contract and a deferred table's writes are unaudited with nothing to notice (D54). **This is the widest decision row in the product** — twenty-two columns, seven of them the engine reads directly — and it is the one a reproducibility record quotes rather than summarises, because a scenario summarised is a scenario that cannot be re-run. **Three settings are a name rather than an id** (`primary_kpi`, `time_step`, `warmup_mode`), which is legitimate for an enum and is the reason D113's KPI vocabulary mattered: a `primary_kpi` naming a measure the engine never emits is a scenario whose objective no run can report.

> **What the database actually permits is wider than the row above.**
> 2 policies here grant access with
> **no predicate at all** (`USING (true)`), so the capability named above is what the
> product intends to check, not what the database enforces:
>
> - `scenarios_auth_all` — `ALL` to `authenticated`
> - `scenarios_anon_all` — `ALL` to `anon`, `authenticated`
>
> Some of these permit **writes**. See PLAN.md D28: the application runs as the
> `anon` role with no auth session and the anon key ships in the frontend bundle, so
> closing these is a migration with an auth model behind it rather than a policy edit.

<details><summary>2 RLS policies</summary>

| Policy | Command | Roles | Added by |
|---|---|---|---|
| scenarios_auth_all | ALL | authenticated | `20260607121406_fcbd47e9-93de-4b3b-988f-7f4718159c91.sql` |
| scenarios_anon_all | ALL | anon, authenticated | `20260919000010_anon_policies_widen.sql` |

</details>

## Where this data is read

| Page | Via | Evidence | Confirmed |
|---|---|---|---|
| `SimulationLab.tsx` | table read | `src/hooks/useScenarios.tsx:70` | yes |
| `SimulationLab.tsx` | table read | `src/hooks/useVerifiableExports.tsx:213` | yes |

Each row says the page READS the table by that path, at that line. It does
not say every column below is displayed there — a column carries its own
lineage only where an explicit `select` names it. `npm run contract:check`
R12 re-opens every evidence line on each run, so an entry cannot go stale
unnoticed.

## Columns

`CSV header` is the name the **user types**, which is not always the column name —
that gap is defect D21. A dash means the column has no CSV origin.

| Column | CSV header | Type | Unit | Required in CSV | Meaning |
|---|---|---|---|---|---|
| `id` 🔑 | — | `uuid` | — | — | Surrogate key. A run binds to this, and so does the reproducibility record. |
| `project_id` | — | `uuid` | — | — | The project this what-if belongs to. |
| `name` | — | `text` | — | — | What a person called it. |
| `description` | — | `text` | — | — | Their own note about it. Defaults to the empty string rather than NULL. |
| `horizon_days` | — | `integer` | — | — | How long the simulated world runs, in days. The engine's own clock is weekly, so this is divided at the boundary rather than stored twice — `normalize-at-promotion` (I3) applied to a decision. |
| `time_step` | — | `text` | — | — | The granularity the scenario was set up at — `day` by default. A NAME, from a small fixed vocabulary, with no table behind it. |
| `warmup_mode` | — | `text` | — | — | Whether the warm-up is detected or fixed. `auto` means the run decides and writes `warmup_detected_at`; `fixed` means `warmup_days` below is used verbatim. |
| `warmup_days` | — | `integer` | — | — | The fixed warm-up, in days. Read only when `warmup_mode` is not `auto` — so a non-default value here with `auto` set is a setting that does nothing, and nothing says so. |
| `replications` | — | `integer` | — | — | How many independent runs. The KPI table's `n` column is the count that ACTUALLY completed, which is a different number whenever a replication fails. |
| `seed` | — | `bigint` | — | — | The root seed. With `crn` below, this is what makes two runs of one scenario comparable rather than merely similar. |
| `crn` | — | `boolean` | — | — | Common random numbers: whether two scenarios draw the same randomness so their difference is the POLICY and not the dice. Default true, and turning it off makes a comparison a different kind of claim. |
| `demand_model` | — | `jsonb` | — | — | How demand is generated when the project's own data does not supply it — kind plus parameters, as JSON. A DECLARED substitution (T2) rather than a silent default, and the default is a Poisson. |
| `disruption_schedule` | — | `jsonb` | — | — | THE WHAT-IF ITSELF — the events, their timing and their targets. Copied from a template when one is applied and diverging from that moment. §4 D112's unresolvable targets arrive here. |
| `recovery_overrides` | — | `jsonb` | — | — | Per-lever changes to the chosen playbook, so a scenario can adjust a strategy without forking the playbook. |
| `stopping_rule` | — | `jsonb` | — | — | When the run stops — a fixed horizon by default, as JSON so a precision-based rule can be expressed without a migration. |
| `primary_kpi` | — | `text` | — | — | The objective this scenario is judged on. A KEY, not a label, and it must be one the engine emits: `fill_rate` by default (§4 D113). |
| `created_by` | — | `uuid` | — | — | The user who set it up, where recorded. NULL is unknown, not nobody. |
| `created_at` | — | `timestamp with time zone` | — | — | When it was created. Server-stamped. |
| `updated_at` | — | `timestamp with time zone` | — | — | When it was last changed, server-stamped. A scenario is mutable, which is why a run binds to a scenario ID AND carries its own copy of what it ran. |
| `recovery_playbook_id` | — | `uuid` | — | — | The playbook this scenario applies, by id. The one place a playbook is referenced by key rather than by name, which is what `scenario_templates.suggested_playbook_name` is not. |
| `from_network` | — | `boolean` | — | — | TRUE when the scenario was created from a network screen rather than from the simulation lab. It records the door a decision came through, which nothing else does. |
| `inherited_validation_id` | — | `uuid` | — | — | The validation run this scenario inherited its data-readiness verdict from, so a re-run does not re-grade an unchanged dataset. NULL where it was never graded — and that is different from graded-and-clean. |

## Each column in full

### `id`

Surrogate key. A run binds to this, and so does the reproducibility record.

| | |
|---|---|
| Type | `uuid`, `NOT NULL`, default `gen_random_uuid()` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20260607121406_fcbd47e9-93de-4b3b-988f-7f4718159c91.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `project_id`

The project this what-if belongs to.

| | |
|---|---|
| Type | `uuid`, `NOT NULL` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20260607121406_fcbd47e9-93de-4b3b-988f-7f4718159c91.sql` |
| References | `public.projects(id)` ON DELETE CASCADE |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `name`

What a person called it.

| | |
|---|---|
| Type | `text`, `NOT NULL` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260607121406_fcbd47e9-93de-4b3b-988f-7f4718159c91.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `description`

Their own note about it. Defaults to the empty string rather than NULL.

| | |
|---|---|
| Type | `text`, default `''` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260607121406_fcbd47e9-93de-4b3b-988f-7f4718159c91.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `horizon_days`

How long the simulated world runs, in days. The engine's own clock is weekly, so this is divided at the boundary rather than stored twice — `normalize-at-promotion` (I3) applied to a decision.

| | |
|---|---|
| Type | `integer`, `NOT NULL`, default `90` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260607121406_fcbd47e9-93de-4b3b-988f-7f4718159c91.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `time_step`

The granularity the scenario was set up at — `day` by default. A NAME, from a small fixed vocabulary, with no table behind it.

| | |
|---|---|
| Type | `text`, `NOT NULL`, default `'day'` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260607121406_fcbd47e9-93de-4b3b-988f-7f4718159c91.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `warmup_mode`

Whether the warm-up is detected or fixed. `auto` means the run decides and writes `warmup_detected_at`; `fixed` means `warmup_days` below is used verbatim.

| | |
|---|---|
| Type | `text`, `NOT NULL`, default `'auto'` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260607121406_fcbd47e9-93de-4b3b-988f-7f4718159c91.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `warmup_days`

The fixed warm-up, in days. Read only when `warmup_mode` is not `auto` — so a non-default value here with `auto` set is a setting that does nothing, and nothing says so.

| | |
|---|---|
| Type | `integer`, `NOT NULL`, default `14` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260607121406_fcbd47e9-93de-4b3b-988f-7f4718159c91.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `replications`

How many independent runs. The KPI table's `n` column is the count that ACTUALLY completed, which is a different number whenever a replication fails.

| | |
|---|---|
| Type | `integer`, `NOT NULL`, default `10` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260607121406_fcbd47e9-93de-4b3b-988f-7f4718159c91.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `seed`

The root seed. With `crn` below, this is what makes two runs of one scenario comparable rather than merely similar.

| | |
|---|---|
| Type | `bigint`, `NOT NULL`, default `42` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260607121406_fcbd47e9-93de-4b3b-988f-7f4718159c91.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `crn`

Common random numbers: whether two scenarios draw the same randomness so their difference is the POLICY and not the dice. Default true, and turning it off makes a comparison a different kind of claim.

| | |
|---|---|
| Type | `boolean`, `NOT NULL`, default `true` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260607121406_fcbd47e9-93de-4b3b-988f-7f4718159c91.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `demand_model`

How demand is generated when the project's own data does not supply it — kind plus parameters, as JSON. A DECLARED substitution (T2) rather than a silent default, and the default is a Poisson.

| | |
|---|---|
| Type | `jsonb`, `NOT NULL`, default `'{"kind":"poisson","lambda":50}'::jsonb` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260607121406_fcbd47e9-93de-4b3b-988f-7f4718159c91.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `disruption_schedule`

THE WHAT-IF ITSELF — the events, their timing and their targets. Copied from a template when one is applied and diverging from that moment. §4 D112's unresolvable targets arrive here.

| | |
|---|---|
| Type | `jsonb`, `NOT NULL`, default `'[]'::jsonb` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260607121406_fcbd47e9-93de-4b3b-988f-7f4718159c91.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `recovery_overrides`

Per-lever changes to the chosen playbook, so a scenario can adjust a strategy without forking the playbook.

| | |
|---|---|
| Type | `jsonb`, `NOT NULL`, default `'{}'::jsonb` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260607121406_fcbd47e9-93de-4b3b-988f-7f4718159c91.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `stopping_rule`

When the run stops — a fixed horizon by default, as JSON so a precision-based rule can be expressed without a migration.

| | |
|---|---|
| Type | `jsonb`, `NOT NULL`, default `'{"kind":"fixed_horizon","max_wall_seconds":600}'::jsonb` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260607121406_fcbd47e9-93de-4b3b-988f-7f4718159c91.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `primary_kpi`

The objective this scenario is judged on. A KEY, not a label, and it must be one the engine emits: `fill_rate` by default (§4 D113).

| | |
|---|---|
| Type | `text`, `NOT NULL`, default `'fill_rate'` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260607121406_fcbd47e9-93de-4b3b-988f-7f4718159c91.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `created_by`

The user who set it up, where recorded. NULL is unknown, not nobody.

| | |
|---|---|
| Type | `uuid` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20260607121406_fcbd47e9-93de-4b3b-988f-7f4718159c91.sql` |
| References | `auth.users(id)` ON DELETE SET NULL |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `created_at`

When it was created. Server-stamped.

| | |
|---|---|
| Type | `timestamp with time zone`, `NOT NULL`, default `now()` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260607121406_fcbd47e9-93de-4b3b-988f-7f4718159c91.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `updated_at`

When it was last changed, server-stamped. A scenario is mutable, which is why a run binds to a scenario ID AND carries its own copy of what it ran.

| | |
|---|---|
| Type | `timestamp with time zone`, `NOT NULL`, default `now()` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260607121406_fcbd47e9-93de-4b3b-988f-7f4718159c91.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `recovery_playbook_id`

The playbook this scenario applies, by id. The one place a playbook is referenced by key rather than by name, which is what `scenario_templates.suggested_playbook_name` is not.

| | |
|---|---|
| Type | `uuid` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20260607125247_efb67f7c-335b-482c-8292-59a47554251d.sql` |
| References | `public.recovery_playbooks(id)` ON DELETE SET NULL |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `from_network`

TRUE when the scenario was created from a network screen rather than from the simulation lab. It records the door a decision came through, which nothing else does.

| | |
|---|---|
| Type | `boolean`, `NOT NULL`, default `false` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260609000021_fix_disruption_bridge.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `inherited_validation_id`

The validation run this scenario inherited its data-readiness verdict from, so a re-run does not re-grade an unchanged dataset. NULL where it was never graded — and that is different from graded-and-clean.

| | |
|---|---|
| Type | `uuid` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20260710000001_model_validations.sql` |
| References | `public.model_validations(id)` ON DELETE SET NULL |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

## Indexes

| Index | Columns | Unique | Added by |
|---|---|---|---|
| `scenarios_project_idx` | `project_id` | no | `20260607121406_fcbd47e9-93de-4b3b-988f-7f4718159c91.sql` |

---

*Generated from data contract `3caef13024f9`, engine `0.2.3`,
sidecar `supabase/contract/scenarios.contract.yaml`, table created by `20260607121406_fcbd47e9-93de-4b3b-988f-7f4718159c91.sql`. No wall-clock date: a generated
page that differs from itself tomorrow cannot be drift-gated.*
