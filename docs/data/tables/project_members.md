<!-- GENERATED by scripts/data-contract/generate.mjs from supabase/contract/project_members.contract.yaml + build/schema.introspected.json + the engine registry. Do not edit by hand: `npm run contract:check` fails on drift. -->

# `project_members`

> **GENERATED** — rendered from the data contract. Edit [`supabase/contract/project_members.contract.yaml`](../../../supabase/contract/project_members.contract.yaml)
> for what the columns mean, or the migrations for what the schema is. Do not edit
> this page: `npm run contract:check` fails when it differs from what the contract
> generates.

**Tier G** — governance — identity, capability, delegation, audit · owned by `platform` · `public.project_members`

**One row is** One person's standing on one project. This is the level of access the platform did not have until WP 2.2 — between "in the organization" (sees every project) and "not in it" (sees none).

Backfilled from `projects.modeler_id` — every project's modeler becomes its owner. That records a fact that already held rather than granting anything new: every write policy in the schema already reads `modeler_id` as the project's owner. `modeler_id` is NOT a declared foreign key to `approved_users`, so the backfill filters on the user existing; a project whose modeler row is gone gets no owner and is visible only to the organization, which is the honest outcome and a §15 question worth asking.

## Uniqueness

| Columns | Source | Constraint |
|---|---|---|
| `project_id` + `user_id` | PRIMARY KEY constraint | — |

## Constraints

These reject the row outright. A value that fails one of them does not arrive
partially or get corrected — the write fails.

| Constraint | Rule | Added by |
|---|---|---|
| `project_members_project_role_check` | `CHECK (project_role IN ('owner','editor','analyst','viewer'))` | `20260915000005_project_membership_and_delegation.sql` |

| Constraint | Kind | Definition |
|---|---|---|
| — | PRIMARY KEY | `PRIMARY KEY (project_id, user_id)` |

## Governance

| | |
|---|---|
| Read capability | any project member |
| Write capability | **no user-facing write path** |
| Minimum project role | `owner` |
| Tier transitions audited | **no** — invariant `audit-actor` is not met here yet |
| Row-level security | enabled |
| Policies on the table | 1 — all carry a predicate |

`write: null` is LITERAL and load-bearing. The table has a SELECT policy and NO insert/update/delete policy, so RLS denies writes by default and the SECURITY DEFINER RPCs are the only way in. That is not an oversight to be tidied up later: D28 says every policy here is PERMISSIVE and permissive policies OR, so a "deny writes" policy added beside anything else would deny nothing. The ABSENCE of a policy is the enforcement. `audited: false` — a membership change writes no audit row; WP 2.3 is where that becomes true, and this table is one of the reasons it matters.

<details><summary>1 RLS policy</summary>

| Policy | Command | Roles | Added by |
|---|---|---|---|
| project_members: read own and co-members | SELECT | all | `20260915000005_project_membership_and_delegation.sql` |

</details>

## Columns

`CSV header` is the name the **user types**, which is not always the column name —
that gap is defect D21. A dash means the column has no CSV origin.

| Column | CSV header | Type | Unit | Required in CSV | Meaning |
|---|---|---|---|---|---|
| `project_id` | — | `uuid` | — | — | The project the membership is on. CASCADEs when the project is deleted. |
| `user_id` | — | `uuid` | — | — | The approved user who holds it. CASCADEs when the account is deleted. |
| `project_role` | — | `text` | — | — | What they may do here — `owner`, `editor`, `analyst` or `viewer`, CHECK-constrained and totally ordered by `project_role_rank()`. A PROJECT vocabulary, and deliberately not `organization_members.org_role` (owner\|admin\|member): they share the word "owner" and mean different things by it. |
| `granted_by` | — | `uuid` | — | — | Who granted the membership. SET NULL rather than CASCADE on purpose — losing the granter's account must not silently delete the grant they made. |
| `expires_at` | — | `timestamp with time zone` | — | — | When the membership lapses. NULL means standing — unlike `delegation_grants`, where an end date is mandatory. `effective_project_role()` applies the comparison so no caller can forget it. |
| `rationale` | — | `text` | — | — | Why the person is on the project, in the granter's words. Optional here and REQUIRED on a delegation: a standing membership is usually self-evident from the project, a temporary elevation never is. |
| `created_at` | — | `timestamp with time zone` | — | — | When the membership was created. Server-stamped. |
| `updated_at` | — | `timestamp with time zone` | — | — | When it was last changed. Server-stamped by DEFAULT only; no trigger maintains it. |

## Each column in full

### `project_id`

The project the membership is on. CASCADEs when the project is deleted.

| | |
|---|---|
| Type | `uuid`, `NOT NULL` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20260915000005_project_membership_and_delegation.sql` |
| References | `public.projects(id)` ON DELETE CASCADE |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `user_id`

The approved user who holds it. CASCADEs when the account is deleted.

| | |
|---|---|
| Type | `uuid`, `NOT NULL` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20260915000005_project_membership_and_delegation.sql` |
| References | `public.approved_users(id)` ON DELETE CASCADE |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `project_role`

What they may do here — `owner`, `editor`, `analyst` or `viewer`, CHECK-constrained and totally ordered by `project_role_rank()`. A PROJECT vocabulary, and deliberately not `organization_members.org_role` (owner\|admin\|member): they share the word "owner" and mean different things by it.

| | |
|---|---|
| Type | `text`, `NOT NULL` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260915000005_project_membership_and_delegation.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | one of owner / editor / analyst / viewer |
| Rendered at | *not yet recorded (WP 5.1)* |

### `granted_by`

Who granted the membership. SET NULL rather than CASCADE on purpose — losing the granter's account must not silently delete the grant they made.

| | |
|---|---|
| Type | `uuid` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20260915000005_project_membership_and_delegation.sql` |
| References | `public.approved_users(id)` ON DELETE SET NULL |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `expires_at`

When the membership lapses. NULL means standing — unlike `delegation_grants`, where an end date is mandatory. `effective_project_role()` applies the comparison so no caller can forget it.

| | |
|---|---|
| Type | `timestamp with time zone` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260915000005_project_membership_and_delegation.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `rationale`

Why the person is on the project, in the granter's words. Optional here and REQUIRED on a delegation: a standing membership is usually self-evident from the project, a temporary elevation never is.

| | |
|---|---|
| Type | `text` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260915000005_project_membership_and_delegation.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

**Substitutions** — every point where a value you did not supply can stand in
for one you did.

| When | The value used | Shown as | Visible where |
|---|---|---|---|
| a row is backfilled from projects.modeler_id | 'backfilled from projects.modeler_id by WP 2.2 — the de-facto owner' | `default` | the stored rationale says so verbatim, so a reader can tell a backfilled row from a granted one |

### `created_at`

When the membership was created. Server-stamped.

| | |
|---|---|
| Type | `timestamp with time zone`, `NOT NULL`, default `now()` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260915000005_project_membership_and_delegation.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `updated_at`

When it was last changed. Server-stamped by DEFAULT only; no trigger maintains it.

| | |
|---|---|
| Type | `timestamp with time zone`, `NOT NULL`, default `now()` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260915000005_project_membership_and_delegation.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

---

*Generated from data contract `9c62127cda63`, engine `0.2.6`,
sidecar `supabase/contract/project_members.contract.yaml`, table created by `20260915000005_project_membership_and_delegation.sql`. No wall-clock date: a generated
page that differs from itself tomorrow cannot be drift-gated.*
