<!-- GENERATED by scripts/data-contract/generate.mjs from supabase/contract/capabilities.contract.yaml + build/schema.introspected.json + the engine registry. Do not edit by hand: `npm run contract:check` fails on drift. -->

# `capabilities`

> **GENERATED** — rendered from the data contract. Edit [`supabase/contract/capabilities.contract.yaml`](../../../supabase/contract/capabilities.contract.yaml)
> for what the columns mean, or the migrations for what the schema is. Do not edit
> this page: `npm run contract:check` fails when it differs from what the contract
> generates.

**Tier G** — governance — identity, capability, delegation, audit · owned by `platform` · `public.capabilities`

**One row is** One thing a user may or may not be permitted to do — a page they may open or a feature they may use. The CATALOG: it says what rights exist, never who holds them. The four grant tables answer that.

THE CATALOG IS DUPLICATED IN TYPESCRIPT. `src/lib/capabilities.ts` carries the same keys as a union type and a list, because the client needs them without a round trip. Two hand-maintained copies of one fact is an I1 violation waiting to drift — `projectMembership.test.ts` asserts the WP 2.2 keys agree, but nothing checks the whole catalog, and the honest count is that the DB has grown keys the TS list learned about only when someone noticed. Generating one from the other is the fix and it belongs with the contract's other generators, not here.

## Uniqueness

| Columns | Source | Constraint |
|---|---|---|
| `key` | column PRIMARY KEY | `capabilities_pkey` |

## Constraints

These reject the row outright. A value that fails one of them does not arrive
partially or get corrected — the write fails.

| Constraint | Rule | Added by |
|---|---|---|
| `capabilities_kind_check` | `CHECK (kind IN ('page','feature'))` | `20260711000002_unified_access_control.sql` |

## Governance

| | |
|---|---|
| Read capability | any project member |
| Write capability | `super_admin` |
| Minimum project role | `viewer` |
| Tier transitions audited | **no** — invariant `audit-actor` is not met here yet |
| Row-level security | enabled |
| Policies on the table | 2 — **1 with no predicate** |

Read is open — every client needs the catalog to render a permissions screen, and the rows contain no tenant data. Writes are service-role only, and in practice happen in migrations: a capability is introduced by the feature that needs it, not by an administrator at runtime.

> **What the database actually permits is wider than the row above.**
> 1 policy here grants access with
> **no predicate at all** (`USING (true)`), so the capability named above is what the
> product intends to check, not what the database enforces:
>
> - `capabilities: read` — `SELECT`
>
> See PLAN.md D28: the application runs as the
> `anon` role with no auth session and the anon key ships in the frontend bundle, so
> closing these is a migration with an auth model behind it rather than a policy edit.

<details><summary>2 RLS policies</summary>

| Policy | Command | Roles | Added by |
|---|---|---|---|
| capabilities: read | SELECT | all | `20260711000002_unified_access_control.sql` |
| capabilities: super write | ALL | all | `20260711000002_unified_access_control.sql` |

</details>

## Columns

`CSV header` is the name the **user types**, which is not always the column name —
that gap is defect D21. A dash means the column has no CSV origin.

| Column | CSV header | Type | Unit | Required in CSV | Meaning |
|---|---|---|---|---|---|
| `key` 🔑 | — | `text` | — | — | The capability's identity, and the string every grant table and every call site names. Page keys are ROUTES (`/policies`); feature keys are snake_case verbs (`data_edit_inputs`). The convention is not enforced by a CHECK. |
| `kind` | — | `text` | — | — | `page` or `feature`, CHECK-constrained. The resolver aggregates the two separately and returns them under separate keys, so the distinction is load-bearing rather than descriptive. |
| `label` | — | `text` | — | — | The human name shown on permission screens. |
| `description` | — | `text` | — | — | One sentence on what holding it lets you do. Nullable. |
| `sort_order` | — | `integer` | — | — | Display order on permission screens. Presentation only. |
| `created_at` | — | `timestamp with time zone` | — | — | When the capability was added to the catalog. Server-stamped. |
| `updated_at` | — | `timestamp with time zone` | — | — | When its metadata last changed. Server-stamped. |

## Each column in full

### `key`

The capability's identity, and the string every grant table and every call site names. Page keys are ROUTES (`/policies`); feature keys are snake_case verbs (`data_edit_inputs`). The convention is not enforced by a CHECK.

| | |
|---|---|
| Type | `text`, `NOT NULL` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20260711000002_unified_access_control.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | unique |
| Rendered at | *not yet recorded (WP 5.1)* |

### `kind`

`page` or `feature`, CHECK-constrained. The resolver aggregates the two separately and returns them under separate keys, so the distinction is load-bearing rather than descriptive.

| | |
|---|---|
| Type | `text`, `NOT NULL` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260711000002_unified_access_control.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | page or feature |
| Rendered at | *not yet recorded (WP 5.1)* |

### `label`

The human name shown on permission screens.

| | |
|---|---|
| Type | `text`, `NOT NULL` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260711000002_unified_access_control.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | non-empty |
| Rendered at | *not yet recorded (WP 5.1)* |

### `description`

One sentence on what holding it lets you do. Nullable.

| | |
|---|---|
| Type | `text` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260711000002_unified_access_control.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `sort_order`

Display order on permission screens. Presentation only.

| | |
|---|---|
| Type | `integer`, `NOT NULL`, default `100` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260711000002_unified_access_control.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `created_at`

When the capability was added to the catalog. Server-stamped.

| | |
|---|---|
| Type | `timestamp with time zone`, `NOT NULL`, default `now()` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260711000002_unified_access_control.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `updated_at`

When its metadata last changed. Server-stamped.

| | |
|---|---|
| Type | `timestamp with time zone`, `NOT NULL`, default `now()` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260711000002_unified_access_control.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

---

*Generated from data contract `f8416eccc038`, engine `0.2.3`,
sidecar `supabase/contract/capabilities.contract.yaml`, table created by `20260711000002_unified_access_control.sql`. No wall-clock date: a generated
page that differs from itself tomorrow cannot be drift-gated.*
