<!-- GENERATED by scripts/data-contract/generate.mjs from supabase/contract/policy_overrides.contract.yaml + build/schema.introspected.json + the engine registry. Do not edit by hand: `npm run contract:check` fails on drift. -->

# `policy_overrides`

> **GENERATED** — rendered from the data contract. Edit [`supabase/contract/policy_overrides.contract.yaml`](../../../supabase/contract/policy_overrides.contract.yaml)
> for what the columns mean, or the migrations for what the schema is. Do not edit
> this page: `npm run contract:check` fails when it differs from what the contract
> generates.

**Tier 4** — decisions — policies, overrides, scenarios · owned by `policy-ui` · `public.policy_overrides`

**One row is** One patch against the project bundle, for one target: this supplier, this material, this customer/product pair. The only tier-4 table with a real natural key — (project, scope, target, family) is UNIQUE, so a target cannot hold two conflicting patches for the same family.

## Uniqueness

| Columns | Source | Constraint |
|---|---|---|
| `project_id` + `scope` + `target_key` + `family` | UNIQUE constraint | — |
| `id` | column PRIMARY KEY | `policy_overrides_pkey` |

## Constraints

These reject the row outright. A value that fails one of them does not arrive
partially or get corrected — the write fails.

| Constraint | Rule | Added by |
|---|---|---|
| `policy_overrides_family_chk` | `CHECK (family IN ('sourcing','inventory','transport','fulfillment','production','recovery','demand'))` | `20260609040000_consolidated_safe.sql` |

| Constraint | Kind | Definition |
|---|---|---|
| — | UNIQUE | `UNIQUE (project_id, scope, target_key, family)` |

## Governance

| | |
|---|---|
| Read capability | any project member |
| Write capability | `data_editing` |
| Minimum project role | `editor` |
| Tier transitions audited | yes |
| Row-level security | enabled |
| Policies on the table | 5 — **2 with no predicate** |

`write` names the capability that exists today. WP 2.2 splits `data_editing` into `data_edit_inputs` (tier 2) and `data_edit_policies` (tier 4); this row becomes `data_edit_policies` then.

> **What the database actually permits is wider than the row above.**
> 2 policies here grant access with
> **no predicate at all** (`USING (true)`), so the capability named above is what the
> product intends to check, not what the database enforces:
>
> - `Authenticated users can read policy overrides` — `SELECT` to `authenticated`
> - `Anon can read policy overrides` — `SELECT` to `anon`
>
> See PLAN.md D28: the application runs as the
> `anon` role with no auth session and the anon key ships in the frontend bundle, so
> closing these is a migration with an auth model behind it rather than a policy edit.

<details><summary>5 RLS policies</summary>

| Policy | Command | Roles | Added by |
|---|---|---|---|
| Authenticated users can read policy overrides | SELECT | authenticated | `20260607055908_b3e74750-d55a-4eb6-8bdc-460bc4cb90a6.sql` |
| Authenticated users can insert policy overrides | INSERT | authenticated | `20260607055908_b3e74750-d55a-4eb6-8bdc-460bc4cb90a6.sql` |
| Authenticated users can update policy overrides | UPDATE | authenticated | `20260607055908_b3e74750-d55a-4eb6-8bdc-460bc4cb90a6.sql` |
| Authenticated users can delete policy overrides | DELETE | authenticated | `20260607055908_b3e74750-d55a-4eb6-8bdc-460bc4cb90a6.sql` |
| Anon can read policy overrides | SELECT | anon | `20260609040000_consolidated_safe.sql` |

</details>

## Columns

`CSV header` is the name the **user types**, which is not always the column name —
that gap is defect D21. A dash means the column has no CSV origin.

| Column | CSV header | Type | Unit | Required in CSV | Meaning |
|---|---|---|---|---|---|
| `id` 🔑 | — | `uuid` | — | — | Surrogate row identifier. The natural key is the UNIQUE constraint, not this. |
| `project_id` | — | `uuid` | — | — | The project this override belongs to. |
| `scope` | — | `text` | — | — | What KIND of thing the override targets — a supplier, a material, a plant, a customer/product pair. Decides how `target_key` is read. |
| `target_key` | — | `text` | — | — | WHICH thing, in the scope's own spelling — a supplier_id, a material_id, or a composite like `customer::product`. |
| `family` | — | `text` | — | — | Which policy family this patch belongs to — the same vocabulary as policy_defaults' jsonb columns. |
| `patch` | — | `jsonb` | — | — | The fields this target overrides, and only those. A field absent from the patch keeps the bundle's value — which is exactly why D1 mattered: writing a field the user never supplied turned a default into a decision. |
| `updated_by` | — | `uuid` | — | — | Who last wrote this patch. |
| `updated_at` | — | `timestamp with time zone` | — | — | When the patch last changed. Server-set. |
| `created_at` | — | `timestamp with time zone` | — | — | When the patch was created. Server-set. |

## Each column in full

### `id`

Surrogate row identifier. The natural key is the UNIQUE constraint, not this.

| | |
|---|---|
| Type | `uuid`, `NOT NULL`, default `gen_random_uuid()` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20260607055908_b3e74750-d55a-4eb6-8bdc-460bc4cb90a6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `project_id`

The project this override belongs to.

| | |
|---|---|
| Type | `uuid`, `NOT NULL` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20260607055908_b3e74750-d55a-4eb6-8bdc-460bc4cb90a6.sql` |
| Read by the engine | `project_map.py::_map_policies -> per-project override fetch` |
| Transform | filter, never read as a value |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `scope`

What KIND of thing the override targets — a supplier, a material, a plant, a customer/product pair. Decides how `target_key` is read.

| | |
|---|---|
| Type | `text`, `NOT NULL` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260607055908_b3e74750-d55a-4eb6-8bdc-460bc4cb90a6.sql` |
| Read by the engine | `project_map.py::_map_policies -> override routing` |
| Transform | matched against the stage the grid wrote it from |
| Validated at ingest | one of the stage scopes the policy grid emits |
| Rendered at | *not yet recorded (WP 5.1)* |

### `target_key`

WHICH thing, in the scope's own spelling — a supplier_id, a material_id, or a composite like `customer::product`.

| | |
|---|---|
| Type | `text`, `NOT NULL` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20260607055908_b3e74750-d55a-4eb6-8bdc-460bc4cb90a6.sql` |
| Read by the engine | `project_map.py::_map_policies -> the node the patch applies to` |
| Transform | parsed per scope |
| Validated at ingest | must name a row that exists; an override for a deleted entity is silently inert |
| Rendered at | *not yet recorded (WP 5.1)* |

### `family`

Which policy family this patch belongs to — the same vocabulary as policy_defaults' jsonb columns.

| | |
|---|---|
| Type | `text`, `NOT NULL` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260607055908_b3e74750-d55a-4eb6-8bdc-460bc4cb90a6.sql` |
| Read by the engine | `project_map.py::_map_policies -> which bundle key to patch` |
| Transform | matched against the bundle's family keys |
| Validated at ingest | one of sourcing / inventory / transport / fulfillment / production / recovery / demand |
| Rendered at | *not yet recorded (WP 5.1)* |

### `patch`

The fields this target overrides, and only those. A field absent from the patch keeps the bundle's value — which is exactly why D1 mattered: writing a field the user never supplied turned a default into a decision.

| | |
|---|---|
| Type | `jsonb`, `NOT NULL`, default `'{}'::jsonb` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260607055908_b3e74750-d55a-4eb6-8bdc-460bc4cb90a6.sql` |
| Read by the engine | `project_map.py::_map_policies -> merged over the family bundle` |
| Transform | shallow-merged over the project bundle's family object |
| When NULL, the engine uses | {} — nothing overridden |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> What may be written here is one rule, `isPrefillPersistable`: an unsaved edit or a value the project data actually said. Never a default. Not every override the engine would honour is reachable from the grid — most per-node fields fall back to project defaults in the mapping (blueprint G1).

### `updated_by`

Who last wrote this patch.

| | |
|---|---|
| Type | `uuid` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20260607055908_b3e74750-d55a-4eb6-8bdc-460bc4cb90a6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

> Governance, not a parameter. WP 2.3 turns it into an audit row naming the actor.

### `updated_at`

When the patch last changed. Server-set.

| | |
|---|---|
| Type | `timestamp with time zone`, `NOT NULL`, default `now()` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260607055908_b3e74750-d55a-4eb6-8bdc-460bc4cb90a6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `created_at`

When the patch was created. Server-set.

| | |
|---|---|
| Type | `timestamp with time zone`, `NOT NULL`, default `now()` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260607055908_b3e74750-d55a-4eb6-8bdc-460bc4cb90a6.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

## Indexes

| Index | Columns | Unique | Added by |
|---|---|---|---|
| `idx_policy_overrides_project` | `project_id` | no | `20260607055908_b3e74750-d55a-4eb6-8bdc-460bc4cb90a6.sql` |
| `idx_policy_overrides_project_family` | `project_id`, `family` | no | `20260607055908_b3e74750-d55a-4eb6-8bdc-460bc4cb90a6.sql` |

---

*Generated from data contract `57ad4b32bb9f`, engine `0.2.3`,
sidecar `supabase/contract/policy_overrides.contract.yaml`, table created by `20260607055908_b3e74750-d55a-4eb6-8bdc-460bc4cb90a6.sql`. No wall-clock date: a generated
page that differs from itself tomorrow cannot be drift-gated.*
