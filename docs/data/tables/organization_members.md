<!-- GENERATED by scripts/data-contract/generate.mjs from supabase/contract/organization_members.contract.yaml + build/schema.introspected.json + the engine registry. Do not edit by hand: `npm run contract:check` fails on drift. -->

# `organization_members`

> **GENERATED** — rendered from the data contract. Edit [`supabase/contract/organization_members.contract.yaml`](../../../supabase/contract/organization_members.contract.yaml)
> for what the columns mean, or the migrations for what the schema is. Do not edit
> this page: `npm run contract:check` fails when it differs from what the contract
> generates.

**Tier G** — governance — identity, capability, delegation, audit · owned by `platform` · `public.organization_members`

**One row is** One user's membership of one organization, and the role they hold IN that organization. Org-level only: it says nothing about which projects inside the organization the user may touch, which is what WP 2.2's `project_members` is for.

Backfilled once by `20260709000002` from `approved_users`, mapping the `admin` app_role to `org_role = 'admin'` and everyone else to `member`. Nothing has maintained it since except the super-admin RPCs, and because `approved_users.organization_id` is the column the backfill keyed on, a user whose uuid org was never stamped has no membership row at all. WP 2.1's backfill narrows that set; §15 is what counts what remains.

## Uniqueness

| Columns | Source | Constraint |
|---|---|---|
| `org_id` + `user_id` | UNIQUE constraint | — |
| `id` | column PRIMARY KEY | `organization_members_pkey` |

## Constraints

These reject the row outright. A value that fails one of them does not arrive
partially or get corrected — the write fails.

| Constraint | Rule | Added by |
|---|---|---|
| `organization_members_org_role_check` | `CHECK (org_role IN ('owner','admin','member'))` | `20260709000002_super_admin_phase1.sql` |

| Constraint | Kind | Definition |
|---|---|---|
| — | UNIQUE | `UNIQUE(org_id, user_id)` |

## Governance

| | |
|---|---|
| Read capability | any project member |
| Write capability | `super_admin` |
| Minimum project role | `viewer` |
| Tier transitions audited | **no** — invariant `audit-actor` is not met here yet |
| Row-level security | enabled |
| Policies on the table | 2 — all carry a predicate |

A member may read only their OWN membership row (`user_id = get_current_user_id()`); there is no policy letting a member enumerate who else is in their organization, and the admin pages that do so run through super-admin RPCs. `audited: false` is the honest record — `admin_add_org_member` and friends log through `log_admin_action`, but membership rows are also written by the `20260709000002` backfill and by nothing else that audits, so the general claim fails. WP 2.3 is where it becomes true.

<details><summary>2 RLS policies</summary>

| Policy | Command | Roles | Added by |
|---|---|---|---|
| org_members: super full | ALL | all | `20260709000002_super_admin_phase1.sql` |
| org_members: read own | SELECT | all | `20260709000002_super_admin_phase1.sql` |

</details>

## Columns

`CSV header` is the name the **user types**, which is not always the column name —
that gap is defect D21. A dash means the column has no CSV origin.

| Column | CSV header | Type | Unit | Required in CSV | Meaning |
|---|---|---|---|---|---|
| `id` 🔑 | — | `uuid` | — | — | Surrogate row identifier. The membership's identity is (org_id, user_id). |
| `org_id` | — | `uuid` | — | — | The organization joined, by uuid. ON DELETE CASCADE — deleting a tenant removes its memberships rather than orphaning them. |
| `user_id` | — | `uuid` | — | — | The approved user who is a member, by uuid. ON DELETE CASCADE. |
| `org_role` | — | `text` | — | — | The member's standing in the organization — `owner`, `admin` or `member`, CHECK-constrained. This is an ORG vocabulary and it is not the project vocabulary: `min_project_role` throughout this contract speaks of viewer / analyst / editor / owner, which WP 2.2 creates on `project_members`. The two share the word "owner" and mean different things by it. |
| `joined_at` | — | `timestamp with time zone` | — | — | When the membership was created. Server-stamped. |

## Each column in full

### `id`

Surrogate row identifier. The membership's identity is (org_id, user_id).

| | |
|---|---|
| Type | `uuid`, `NOT NULL`, default `gen_random_uuid()` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20260709000002_super_admin_phase1.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `org_id`

The organization joined, by uuid. ON DELETE CASCADE — deleting a tenant removes its memberships rather than orphaning them.

| | |
|---|---|
| Type | `uuid`, `NOT NULL` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20260709000002_super_admin_phase1.sql` |
| References | `public.organizations(id)` ON DELETE CASCADE |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `user_id`

The approved user who is a member, by uuid. ON DELETE CASCADE.

| | |
|---|---|
| Type | `uuid`, `NOT NULL` |
| Grain | `identifier` |
| Unit | dimensionless |
| Added by | `20260709000002_super_admin_phase1.sql` |
| References | `public.approved_users(id)` ON DELETE CASCADE |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

### `org_role`

The member's standing in the organization — `owner`, `admin` or `member`, CHECK-constrained. This is an ORG vocabulary and it is not the project vocabulary: `min_project_role` throughout this contract speaks of viewer / analyst / editor / owner, which WP 2.2 creates on `project_members`. The two share the word "owner" and mean different things by it.

| | |
|---|---|
| Type | `text`, `NOT NULL`, default `'member'` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260709000002_super_admin_phase1.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | one of owner / admin / member |
| Rendered at | *not yet recorded (WP 5.1)* |

**Substitutions** — every point where a value you did not supply can stand in
for one you did.

| When | The value used | Shown as | Visible where |
|---|---|---|---|
| a member row is inserted without a role | 'member' — the least of the three | `default` | the column default, and the admin member list renders the resulting role verbatim |

> NOT READ BY THE ACCESS LAYER TODAY. `capabilities_for_user()` resolves role -> org -> user through `approved_users.role` and the capability tables; no policy and no RPC consults `org_role`. An organization `admin` therefore holds no more than a `member` does, which is the gap WP 2.2 closes when it extends the resolver. Recorded rather than implied, per §5 T1.

### `joined_at`

When the membership was created. Server-stamped.

| | |
|---|---|
| Type | `timestamp with time zone`, `NOT NULL`, default `now()` |
| Grain | `metadata` |
| Unit | dimensionless |
| Added by | `20260709000002_super_admin_phase1.sql` |
| Read by the engine | **not traced** |
| Validated at ingest | — |
| Rendered at | *not yet recorded (WP 5.1)* |

---

*Generated from data contract `6305d7ea6f57`, engine `0.2.3`,
sidecar `supabase/contract/organization_members.contract.yaml`, table created by `20260709000002_super_admin_phase1.sql`. No wall-clock date: a generated
page that differs from itself tomorrow cannot be drift-gated.*
